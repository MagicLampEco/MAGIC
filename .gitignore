node_modules/
dist/
.env
*.env
.DS_Store

# Nhà riêng agent + worktree tạm — KHÔNG đẩy lên org repo
_Agents/
.claude/

# Khoá / bí mật — chặn sẵn, không bao giờ commit
*.key
*.pem
*.pat
*.mnemonic

# Aiken build artifacts
**/onchain/build/
**/onchain/plutus.json
MagicSDK/node_modules

# Tệp nội bộ từng máy — ranh giới chia sẻ nằm ở BOUNDARIES.md (được track)
CLAUDE.md

# Trang thai deploy cuc bo theo mang (gia tri cong khai, ban ghi chuan la scripts/DEPLOYED.md)
scripts/state.*.sh
