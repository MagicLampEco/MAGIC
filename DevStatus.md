# DevStatus — module nào đang sống, đang chết, đang mồ côi

> **Vai của tệp này:** trả lời "**cái gì đang đúng LÚC NÀY**". Chuyện *đã xảy ra* nằm ở
> [`ChangeLog.md`](ChangeLog.md); mô hình *phải thế nào* nằm ở
> [`SPEC/MagicLamp-Tripletoken-Feat-(Vi).md`](SPEC/MagicLamp-Tripletoken-Feat-(Vi).md).
>
> **Đây là nơi DUY NHẤT ghi trạng thái + số kiểm.** Đừng chép số test sang README, sang
> spec, sang PR body. Bài học đo được: một con số test từng sống ở 28 vị trí với 12 giá
> trị khác nhau, và bản sai lọt ra trang công khai. Cần số thì trỏ về đây, hoặc chạy lệnh.

**Đo lúc:** 2026-08-13 · **trên nhánh** `feat/converge-tripletoken-code`.
Số dưới đây là ảnh chụp — hết hạn ngay khi có commit mới. Lệnh kiểm chứng nằm ngay cạnh.

---

## Đang sống — nằm trong mô hình ba-token

| Module | Vai | vitest | `aiken check` |
|---|---|---|---|
| `ProtocolUtils` | thư viện dùng chung (hằng, Q-format, BigInt) | 26 | — |
| `InstantGen` | sinh MAGIC theo yêu cầu, vault hợp nhất PHA-2 | 55 | 85 |
| `ScheduleGen` | hợp đồng kỳ hạn, rate khoá lúc commit, 16 shard | 43 | 58 |
| `UMKeeper` | cập nhật hệ số cầu mạng UM mỗi epoch | 20 | 10 |
| `ConsumeMAGIC` | tiêu thụ MAGIC (đốt theo giá nghiệp vụ) | 71 | 100 |
| `ConsumeMAGIC/pricing` | `@magiclamp/consumemagic-pricing` — bộ định giá | 62 | (dùng chung) |
| `MagicSDK` | mặt tiền cho bên tích hợp | 52 | — |
| `GetMAGIC` | cổng vào, Phase 1 (chưa nối vault) | 53 | 23 |
| `Paymaster` | trả phí hộ (SponsorMeter) | 26 | 28 |
| `FlowRate` | điều tiết nhịp | 19 | (không có `aiken.toml`) |
| `PrepaidGen` | cửa sinh thứ ba — người dùng trả CARP | 75 | 72 |

```bash
# vitest một module
cd <Module>/offchain && npm test          # ConsumeMAGIC/pricing và MagicSDK: npm test tại chính thư mục đó

# aiken — 1.1.21 không in gì khi bị pipe
cd <Module>/onchain && script -q /tmp/out.txt aiken check && cat /tmp/out.txt
```

> Hai số vừa đổi so với bản 2026-08-09: `ScheduleGen` 39 → **41** vitest, `MagicSDK` 51 → **52**.
>
> **Đo lại 2026-08-16** (lệnh + output thô, không chép từ trí nhớ):
> `GetMAGIC/onchain` → `Summary 18 checks, 0 errors, 1 warning` (trước: `0 checks`) và
> `GetMAGIC/offchain` → `Tests 53 passed (53)` (trước: 41). Nợ #16 **đóng phần GetMAGIC**;
> phần Nợ #26 (khuôn thông điệp không đơn ánh) cũng đã vá. Cả hai đã commit ở `ef0338b4`
> (kèm tệp mới `GetMAGIC/onchain/lib/getmagic/utils_test.ak`) — **chưa đẩy lên remote**.
> `ConsumeMAGIC` 67 là số **sau** khi vá Nợ #27 — trước đó vitest chỉ nạp được 31 mà không
> ai thấy là mất 36.
>
> **Đo lại 2026-08-27** — chạy đủ từng gói, lấy dòng tổng thô:
> `aiken check` **366** (InstantGen 85 · ScheduleGen 58 · ConsumeMAGIC 100 · GetMAGIC 23 ·
> Paymaster 28 · PrepaidGen 72), 0 lỗi 0 cảnh báo. `vitest` **510** (InstantGen 55 ·
> ScheduleGen 43 · ConsumeMAGIC/offchain 71 · ConsumeMAGIC/pricing 62 · GetMAGIC 53 ·
> MagicSDK 52 · Paymaster 26 · ProtocolUtils 26 · FlowRate 19 · UMKeeper 20 ·
> ProfileChange 8 · PrepaidGen 75). Bảng trên đã sửa theo số này; các số cũ trong bảng
> có từ trước khi vá Nợ #35/#36/#41/#42 và trước khi PrepaidGen vào git.
> Cảnh báo `self_ref` không dùng ở `GetMAGIC` cũng hết — chính chỗ đó là Nợ #41.

> **Ghim phụ thuộc 2026-08-26 — stdlib ghim theo COMMIT SHA, không theo tag.** Cả 10
> `aiken.toml` + 10 `aiken.lock` (8 module sống + 2 trong `Legacy/genmagic-v3.3/`) đổi
> `aiken-lang/stdlib` từ `v3.1.0` sang `7d5cee54b2bb4eea211ae3bd806c7c39e5fd899d`. Lý do:
> tag git **di chuyển được** ⇒ dựng lại cùng một commit ở hai thời điểm có thể ra hai
> `plutus.json` khác nhau ⇒ đổi script hash ⇒ **đổi địa chỉ**, tiền ở địa chỉ cũ không ai
> mở được. Tra SHA bằng
> `git ls-remote --tags https://github.com/aiken-lang/stdlib.git 'v3.1.0*'` → hai dòng:
> `933c85e1…` (tag object) và `7d5cee54…` (`v3.1.0^{}` = commit). **Phải lấy dòng `^{}`** —
> tag này là annotated + GPG-signed (KtorZ, 2026-04-24), ghim nhầm sha của tag object là
> ghim vào một đối tượng không phải commit.
>
> Chứng minh bản vá không đổi gì: `aiken build` 10 module trước/sau, đọc hash bằng
> `jq -r '.validators[]|"\(.title) \(.hash)"'`, `diff` **rỗng** trên 37 mục validator
> (`sha256` hai bảng bằng nhau: `51ed1e80…`). Thêm một chứng cứ độc lập: giải nén hai
> zipball trong cache aiken (`…-v3.1.0.zip` tải 2026-05-17 và
> `…-7d5cee54….zip` tải 2026-08-26) rồi `diff -r` → **không khác byte nào**, tức tag chưa
> trôi tính tới hôm nay. `aiken check` 10 module: 399 checks, 0 error.
>
> Còn hở, nằm ngoài tầm với: `aiken.lock` của **chính stdlib** ghim
> `aiken-lang/fuzz = "v2.2.0"` — cũng là tag. Build của repo này không kéo `fuzz` (log
> `Fetched 1 package`) nên không ảnh hưởng script hash; nhưng module nào khai `fuzz` làm
> phụ thuộc thì lỗ hổng tag mở lại.

## Đo trên testnet — chuỗi deploy chạy thật, không phải test đơn vị

> Bảng trên là test **đơn vị**: mã tự chấm mã. Bảng dưới là tx **thật** trên chuỗi thật, nơi
> validator chạy dưới ledger chứ không dưới mock. Hai thứ nói hai điều khác nhau, và bài học
> đắt nhất của repo này nằm đúng ở khe giữa hai bảng: `ig_pos_*` xanh 55/55 trong khi
> InstantGen chưa từng cấp nổi 1 nanogic trên chuỗi (Nợ #19).

**Đo lúc:** 2026-08-13, chạy trọn `bash scripts/run_wakeme_e2e.sh <Preview|Preprod>`
(00 → 0a…0e → 07 → ScheduleCommit → ScheduleFire → 05 → InstantGen). Tx hash + địa chỉ:
[`scripts/DEPLOYED.md`](scripts/DEPLOYED.md).

| Bước | Preview | Preprod | Ý nghĩa |
|---|---|---|---|
| Dựng LAMP + UM + Shard + beacon | ✅ | ✅ | chuỗi phụ thuộc đi hết, không kẹt bước nào |
| Công bố script tham chiếu CIP-33 | ✅ | ✅ | bắt buộc, không phải tuỳ chọn — Nợ #20 |
| Tạo vault ScheduleGen | ✅ | ✅ | NFT one-shot mint cùng tx (INV-VAULT-IDENTITY) |
| **ScheduleCommit** | ✅ | ✅ | `gen_schedules` ghi được, shard giảm, rate khoá theo T8 |
| **ScheduleFire** | ⏳ | ⏳ | `No eligible fires: next fire at epoch 20679, current=20677` — **chưa tới hạn, đúng thiết kế**, không phải lỗi |
| Tạo vault InstantGen | ✅ | ✅ | |
| **InstantGen** | ❌ | ❌ | `reward=0 cap_surplus=33333333333 cap_pp=0 (consumed_credit=0)` — Nợ #19 |

Ba điều bảng này nói mà bảng test đơn vị không nói được:

1. **ScheduleGen là cửa sinh MAGIC DUY NHẤT đã chứng minh được trên chuỗi.** Hai mạng, hai
   ngày, tx thật.
2. **Không nghiệm thu được ScheduleGen trọn vòng trong một buổi.** `SCHEDULE_DELAY = 2` epoch,
   `ms_per_epoch` testnet = 86 400 000 (1 ngày) ⇒ chờ ~2 ngày sau commit mới fire được.
3. **InstantGen hỏng giống hệt nhau trên 4 vault khác nhau, 2 mạng, 2 ngày.** `cap_surplus > 0`
   chứng minh beacon dựng-tạm ĐÃ mở cổng §6.3 — nên đây không phải thiếu cấu hình.

**Kích thước script, đo lại bằng apply-param thật 2026-08-13** (thay con số 16643 cũ):

| Script ScheduleGen | chưa apply | đã apply |
|---|---|---|
| `vault.vault.spend` | 11 210 B | 11 302 B |
| `vault.shard.spend` | 5 131 B | 5 169 B |
| **tổng** | 16 341 B | **16 471 B** |

Trần tx Cardano là **16 384 B**. Chưa apply thì hai script **vừa lọt** (dư 43 B); apply-param
đẩy lên **vượt 87 B**. Nên chính bước apply-param là thứ đá ScheduleGen qua trần — không phải
kích thước mã. Tx thật đính kèm cả hai đo được 17 303 B (16 471 script + ~832 B phần tx).
Kiểm lại: `applyParamsToScript` trên `ScheduleGen/onchain/plutus.json`, tham số hình dạng thật
(policy 28 B · `744c414d50` · policy 28 B · `86400000`).

## Mồ côi — còn dùng được nhưng chưa có nhà rõ ràng

| Module | vitest | `aiken check` | Vướng ở đâu |
|---|---|---|---|
| `Consolidate` | 12 | 21 | là script hash RIÊNG nên vault ở địa chỉ InstantGen không bao giờ chạy được validator của nó — tàn dư mô hình "4 module chung một vault" |
| `ProfileChange` | 8 | 13 | như trên |
| `AppEconomics` | 54 | — | `SPEC.md` còn bám GenMAGIC v3.3 |

> Bản trước ghi hai module đầu "không có `aiken.toml` riêng để build standalone". **Sai** — cả
> hai đều có `onchain/aiken.toml` được track, và `aiken build` chạy 0 lỗi. Vật cản được nêu
> không tồn tại, nên D4 suýt treo vì lý do sai: người gỡ treo sẽ đi tạo một tệp đã có sẵn thay
> vì xử lý vật cản thật (script hash riêng). Bản trước cũng ghi `AppEconomics/SPEC.md` "tự khai
> NORMATIVE" — dòng đó đã được gỡ khỏi tệp kia rồi.

**Cả ba đều KHÔNG có NFT danh tính.** `vault_consolidate.ak` và `vault_profile.ak` chỉ có
handler `spend`, không có `mint`, và đếm được 0 tham chiếu tới NFT vault. Chưa hại vì chưa có
tài sản đi qua; nhưng nếu D4 chốt hội tụ thì phải nối `INV-VAULT-IDENTITY` **trước**, không thì
mở lại lỗ 2-ADA-datum-bịa.

Ba module này **chưa được quyết**: hội tụ vào mô hình ba-token, hay **xoá**. (Không còn lựa
chọn "dời `Legacy/`" — chủ nhân đã bãi bỏ `Legacy/` ngày 2026-08-09; xem `## Không được xoá`.)
Chờ chốt PM/tư-cách trong spec canonical rồi mới xử — dọn sớm là dọn mù.

## Đã chết — ở `Legacy/genmagic-v3.3/`, không đọc

`SnapshotGen` · `VacuumGen` · 10 báo cáo testnet · `SnapshotGen-Simulator.HTML` ·
`DEVELOPER_GUIDE.md` · 5 tệp script phục vụ riêng hai module trên.
Lý do từng cái: [`Legacy/README.md`](Legacy/README.md).

---

## Còn nợ — biết rõ, chưa làm

| # | Việc | Vì sao chưa |
|---|---|---|
| 1 | Chưa deploy mạng nào | cần credential + quyết định của chủ nhân |
| 2 | `BackingBeacon` (§6.3) chưa có bytes thật | chờ CarpetMint deploy lại — policy id của CARP **sẽ đổi**, đang để all-zero fail-closed nên InstantGen đóng cửa an toàn |
| 3 | Chưa có CI | không có `.github/`; mọi kiểm hiện chạy tay |
| 4 | `AppEconomics` chưa hội tụ | xem mục mồ côi |
| 5 | Nguồn `PrepaidGen` **chưa mất** — đang treo trong `refs/stash@{0}` | 24 tệp nguồn (`prepaid.ak`, `fund_nft.ak`, trọn `offchain/src`, `tests/`, `DESIGN.md`). Bản ghi cũ ở đây kết luận "đã mất" vì `git log --all --diff-filter=A` trả rỗng — nhưng `--all` **không quét `refs/stash`**, nên rỗng ở đó không có nghĩa là mất. Nguyên nhân gốc: code viết trên `feat/genmagic-v0.2-handoff`, **chưa từng commit**, bị `git stash` tự hứng lúc chuyển nhánh 2026-07-30 và không ai `pop` lại. Đã neo bằng tag `preserve/prepaidgen-stash-2026-07-30` để `git stash clear/drop` không xoá được. Kiểm: `git ls-tree -r --name-only preserve/prepaidgen-stash-2026-07-30^{commit}^3 \| grep -c '^PrepaidGen/'` → 24 |
| 6 | **InstantGen chưa bao giờ cấp được 1 nanogic** | trần thứ ba còn là `0.5 × Σ(gen_schedules)` kiểu cũ; vault thường có `gen_schedules = []` ⇒ trần 0 ⇒ `expect grant > 0` fail. SPEC §6.3 đòi `⌊L_avail × RATE_REF_Q / Q⌋`. **Chờ chủ nhân chốt** — phải vá CÙNG LÚC với `INV-INSTANT-LOCK`, không thì mở đường flash-rent LAMP |
| 7 | `ConsumeMAGIC` chưa có `buildPostPriceTx` | bất biến "giữ nguyên phần non-ADA của beacon" chưa có bên off-chain nào thực thi |
| 8 | `buildConsumeTx` mới dựng 1 Engage input | on-chain là bất biến AGGREGATE qua N input — tập con hợp lệ, nhưng chưa gộp được nhiều thread |
| 9 | Không có đường đóng thread Engage / vault | mint ép `qty == 1` nên burn bất khả ⇒ min-ADA khoá vĩnh viễn mỗi thread. Cố ý, nhưng là quyết định cần biết |
| 10 | `UMKeeper/offchain/src/keeper.ts` **chưa từng chạy với node thật** | Tệp này trước đây **không biên dịch được** (4 lỗi kiểu) và không ai biết: gói không có `tsconfig.json` nên `tsc --noEmit` chưa từng chạy, còn vitest chỉ chạm `math.ts`. Nó cũng import `@lucid-evolution/lucid` mà `package.json` không khai. Đã vá cả ba (thêm `tsconfig.json`, khai dep, dựng cặp `Data.Static` mà lucid-evolution bắt buộc) ⇒ nay `npm run typecheck` xanh, `npm test` 20/20. Nhưng **xanh kiểu ≠ chạy đúng**: `getEpochStats` vẫn là bản giả trả số cố định, và chưa có lần chạy nào với Blockfrost. Liên quan D7 |
| 11 | ~~4 gói + `scripts/` chưa có `tsconfig.json`~~ **ĐÃ VÁ 2026-08-12, trừ `scripts/`** | Bản ghi cũ ở đây **sai hai chiều**: nó bỏ sót `FlowRate/offchain` và `ProtocolUtils` (gói P8 chuẩn, 25 tệp import). Nay cả sáu đều có `tsconfig.json` + script `typecheck`. Năm gói xanh 0 lỗi. Còn hai chỗ đỏ, ghi riêng ở #12 và #13 |
| 12 | ~~`scripts/` typecheck **18 lỗi**~~ **0 lỗi 2026-08-17 — nhưng con số này ĐO ĐƯỢC HAI GIÁ TRỊ tuỳ máy** | 🔴 Đây mới là điều đáng ghi, không phải con số 0. `scripts/tsconfig.json` kéo cả `../MagicSDK/src/*` vào phạm vi, mà `MagicSDK` **không có `node_modules`**. Cùng một cây mã cho hai kết quả: chưa cài MagicSDK → **13 lỗi**, trong đó 10 `TS2307 Cannot find module '@lucid-evolution/lucid'` (nhiễu môi trường) và **3 `TS2365` trộn `number` với `bigint`** — đúng cái `BOUNDARIES.md` gọi là "lỗi tràn số đang chờ xảy ra"; cài MagicSDK xong (`npm install` tại `MagicSDK/`) → **0 lỗi**, cả ba `TS2365` biến mất vì `@magiclamp/protocol-utils` giải được kiểu nên `bigint` không còn suy thành `number`. Nghĩa là **cổng typecheck của `scripts/` chỉ nói thật khi hàng xóm đã cài** — cùng lớp Nợ #27, và đúng lý do cổng CI phải cài phụ thuộc `file:` theo thứ tự tôpô trước khi kiểm. Đừng ghi "0 lỗi" mà không ghi kèm điều kiện. *(Lịch sử: 19 → 18 → 13 → 0.)* Gói duy nhất chạm ví thật. Nay đã có `tsconfig.json` + `npm run typecheck`, và cổng bắt được ngay một lỗi THẬT: `test/consume_only.ts` dựng `EngageDatum` thiếu `consumed_nanogic` ⇒ tx dựng ra bị validator từ chối (W-CM-12) — đã vá. 19 lỗi còn lại thuộc hai lớp: (a) `Data.to(x, Schema)` truyền lược đồ thay vì kiểu tĩnh — cùng lớp đã vá ở UMKeeper; (b) hai bản `@lucid-evolution/lucid` (`scripts/node_modules` vs `InstantGen/offchain/node_modules`) cho hai định danh kiểu khác nhau. Con số này chỉ được phép GIẢM |
| 13 | ~~`ProfileChange/offchain` typecheck **3 lỗi**~~ **ĐÃ VÁ 2026-08-17** | `src/profile.ts` import `Tx` — lucid-evolution 0.4.30 không export tên đó; và một `Data.to` truyền lược đồ. Trước 2026-08-12 gói không có `tsconfig.json` nên chưa ai thấy. Nay `npm run typecheck` in đúng hai dòng banner rồi im — **0 lỗi**. Số phận module vẫn buộc vào D4 (hội tụ hay xoá), nhưng nó không còn là nợ kỹ thuật |
| 14 | 🔴 `validate_burn_batch` **không đòi ConsumeMAGIC co-spend** — liên kết giá MỘT CHIỀU | `InstantGen/onchain/validators/vault.ak:851-936`: auth = chủ HOẶC `personal_delegate`, và không ràng buộc input nào ở địa chỉ `consume`, không đọc PriceParam beacon. `consume.ak:95` ép chặt chiều consume→vault; chiều ngược lại bỏ ngỏ. Hệ quả: một delegate ký được tx BurnBatch trần trụi, không giao dịch vụ gì, đốt sạch MAGIC của chủ vault (LAMP và ADA vẫn an toàn — hai chốt dưới chặn đúng). `personal_delegate` hiện là `Option<ByteArray>` trần: không hạn mức, không phạm vi op, không hạn dùng. Vá đụng validator ⇒ xem D8 |
| 15 | Không ai kiểm chữ ký chủ trên nhánh **spend** của Engage | `ConsumeMAGIC/onchain/validators/consume.ak` dùng `extra_signatories` đúng MỘT lần, ở nhánh **mint** (`:253`). Nhánh spend chỉ ép `owner` được bảo toàn. Hôm nay đây là TÍNH NĂNG (nó cho phép Feecover ứng trước cho ví 0 MAGIC — đã trả lời PhoenixKey 2026-08-12), nhưng nó cũng nghĩa là ai cũng bơm được `consumed_count`/`consumed_nanogic` của người khác. Sẽ thành lỗ ngay khi attribution/ISPO đọc hai trục đó |
| 16 | `GetMAGIC/onchain` biên dịch sạch nhưng **0 test Aiken** | `aiken check` → `0 checks, 0 errors`. Không có `test` nào định nghĩa trong validator. Biên dịch được không có nghĩa có bài kiểm — cùng họ với Nợ #10 |
| 17 | `Paymaster`: `ada_per_magic_q` không có chặn âm | `paymaster.ak:111` chặn `lamp_per_magic_q` bằng sàn giao thức, `ada_per_magic_q` thì không. Với giá âm, Aiken `/` (làm tròn xuống) cho `-1` còn TS BigInt `/` (cắt về 0) cho `0` ⇒ off-chain dựng tx mà on-chain từ chối. Fail-closed, không mất tiền, nhưng là chỗ P8 lệch theo dấu |
| 18 | ~~`scripts/config.ts:75-76` khai lại `SHARD_COUNT`/`SHARD_CAP` độc lập~~ **ĐÃ VÁ 2026-08-17** | Hai nguồn cho một sự thật — đổi một bên là lệch im lặng. Nay `config.ts:10` `import { SHARD_COUNT, SHARD_CAP } from "../ScheduleGen/offchain/src/constants.js"` và `:78-79` chỉ chuyển tiếp. Ba nơi đã đối chiếu khớp: `ScheduleGen/offchain/src/constants.ts:31-32` (`16`, `450_000_000_000_000n`) ↔ `ScheduleGen/onchain/lib/magiclamp/protocol/constants.ak:46,50` (`16`, `450_000_000_000_000`) ↔ `scripts/`. Còn lại đúng hai nguồn, và đó là cặp TS↔Aiken mà P8 bắt buộc phải có |
| 19 | 🔴 **InstantGen không mở được cho BẤT KỲ vault nào** — vòng khép kín trong chính mã | Đo thật trên Preview **và** Preprod 2026-08-12: `reward=0 cap_surplus=33333333333 cap_pp=0`. Chuỗi đóng: genesis ép `gen_schedules == []` (`vault.ak:281`), sáu nhánh spend đều ép `output.gen_schedules == input.gen_schedules` (`:455,559,808,921,1033,1093`), và `VaultRedeemer` của InstantGen **không có nhánh nào GHI** `gen_schedules` (chỉ có `ScheduleCommit` — mà nhánh đó nằm ở validator ScheduleGen, script hash KHÁC, địa chỉ KHÁC). ⟹ `compute_cap_pp([]) = 0` vĩnh viễn ⟹ `expect grant > 0` (`vault.ak:421`) luôn hỏng. Test đơn vị `ig_pos_*` xanh vì chúng tự dựng datum có `gen_schedules` — trạng thái mà đường mint cấm. Đây KHÔNG phải lỗi cấu hình: beacon dựng-tạm đã mở cổng thặng dư (cap_surplus > 0) mà vẫn hỏng. Vá thuộc D1. **2026-08-13, soát lại từ nguyên lý gốc: KHÔNG phải một khoá, mà HAI khoá độc lập.** Genesis ép cả ba trường cùng lúc (`vault.ak:272` `magic_batches == []`, `:276` `consumed_credit == 0`, `:281` `gen_schedules == []`). Khoá thứ hai: `consumed_credit` chỉ tăng ở nhánh `BurnBatch` (`:929`), mà `BurnBatch` chết ngay ở `expect total_burned > 0` (`:909`) khi `magic_batches` rỗng, mà `magic_batches` chỉ được ghi ở chính nhánh `InstantGen` (`:469`) — vòng tự-tham-chiếu ⟹ `reward = 0` vĩnh viễn, độc lập với `cap_pp`. Số đo in ra đúng cả hai: `reward=0 … cap_pp=0 (consumed_credit=0)`. Nghĩa là kể cả vá `cap_pp` thì InstantGen VẪN đóng — vault InstantGen không có nguồn MAGIC nào để tiêu trước. Nguồn MAGIC duy nhất còn sống là `BatchSource::Schedule` (`ScheduleFire`), nằm ở validator kia |
| 20 | ScheduleGen **bắt buộc** script tham chiếu CIP-33, không phải tuỳ chọn | Đính kèm cả `vault` lẫn `shard` vào một tx cho **17303 byte > 16384** (đo thật, Preview). Đo lại 2026-08-13 bằng apply-param thật (bản ghi cũ ở đây ghi 16643 — **sai**): hai script chưa apply cộng lại 16 341 B (dư 43 B dưới trần), **đã apply 16 471 B — vượt 87 B**. Tức chính bước apply-param đá nó qua trần, không phải kích thước mã. Nên cả bước công bố cũng phải tách hai tx. `scripts/deploy/06_publish_ref_scripts.ts` + `refScriptUtxos` trong `ScheduleGen/offchain/src/schedule.ts`. Bên tích hợp nào định gộp tx với vault MAGIC phải tính phần ngân sách byte này |
| 21 | Vault InstantGen và vault ScheduleGen là **hai địa chỉ**, một người dùng cần hai vault | Cùng `VaultDatum` 17 trường, nhưng redeemer rời nhau và chỉ số lệch: `WithdrawLamp` = 4 ở Instant, 3 ở Schedule; `SetDelegate` = 5 và 4. Một UTxO nằm ở một địa chỉ nên không vault nào chạy được cả hai cửa. "Hợp nhất Instant+Schedule" (PHA-2) mới hợp nhất DATUM, chưa hợp nhất SCRIPT. Cùng họ với vướng mắc của `Consolidate`/`ProfileChange` ở mục Mồ côi |
| 22 | Chuỗi deploy **không bước nào chờ xác nhận** trước 2026-08-12 | Năm script `deploy/*.ts` submit rồi trả ngay, nên bước sau tiêu phải UTxO cũ ⇒ `BadInputsUTxO`. Đã thêm `awaitTx` cả năm. Còn một lớp nữa chưa vá tận gốc: Blockfrost trả danh sách UTxO trễ vài giây **sau khi** `awaitTx` xong, nên 04/06 phải có vòng thử lại. Bước nào chưa có vòng đó vẫn hỏng ngẫu nhiên |
| 23 | Ví deploy hết UTxO thuần ADA là **mọi tx script** bị từ chối | `CollateralContainsNonADA`: collateral không được mang native token, mà ví dùng lâu sẽ gom hết ADA vào UTxO có token. Đã thêm `scripts/prepare_wallet.ts` và gọi trước mỗi cụm bước. Đây là lỗi hình dạng ví, không phải lỗi validator — nhưng nó trông y hệt |
| 24 | 🔴 `origin/main` đã tụt thành nhánh cụt — thiếu `InstantGen/`, `UMKeeper/`, `SPEC/`, `DevStatus.md`, `BOUNDARIES.md` | PR #24 dời `InstantGen` vào `Legacy/stale-genmodel-2026-07/` và `UMKeeper` vào `Legacy/`, trong khi `UMKeeper` là **apply-param #3 và #4** của `InstantGen/onchain/validators/vault.ak` (`um_nft_policy`, `um_script_hash` — kiểm bằng `plutus.json`). Kiểm: `git ls-tree -r --name-only origin/main \| grep -c '^InstantGen/'` → **0**; `git ls-tree --name-only origin/main SPEC/` → rỗng. Hệ quả: issue #26 gọi `SPEC/MagicLamp-Tripletoken-Feat-(Vi).md` là spec canonical mà người đứng ở `main` **không mở được**, và `merge-tree origin/main HEAD` cho **109 xung đột**. **Không bắt đầu việc mới trên `main`**. 🔴 **ĐÍNH CHÍNH 2026-08-17 — câu "gần hết 109 xung đột đến từ một câu hỏi chưa chốt: InstantGen sống hay chết" là do BÊN NÀY ĐỌC LỆCH, xoá khỏi hồ sơ.** Chưa bao giờ có câu hỏi đó. Mô hình chủ nhân giữ nguyên từ đầu: **đúng ba** cửa sinh — Schedule và Instant sinh từ số dư LAMP trong vault người dùng, Prepaid thì người dùng trả CARP. Chuỗi sai: thân PR #24 viết "*tên `InstantGen` và `ScheduleGen` vẫn sống trong mô hình mới, nhưng **cài đặt** cũ thì không*"; bên này gộp "cài đặt cũ" thành "cửa sinh", rồi lấy việc `InstantGen/` vắng mặt trên `origin/main` (hệ quả của PR #23 chưa merge, không phải một quyết định) làm bằng chứng độc lập, tự viết câu hỏi vào PR #30, ba ngày sau đọc lại câu hỏi của chính mình như nguồn bên ngoài. Tuân đã trả lời "sống" từ 2026-08-14 và phân rã 109 xung đột theo module: SnapshotGen 81 · VacuumGen 65 · InstantGen 8 · UMKeeper 4. Kho đã cảnh báo ở `_Agents/topics/version-audit-2026-07-30.md:78` từ 30/7. Nguyên nhân thật của 109 xung đột đã đo và ghi ngay bên dưới (82 % sắp xếp `Legacy/`, 27 tệp nội dung quy về D12) — không dính gì tới cửa sinh. Việc InstantGen còn treo thật là **Nợ #19, #14, #21 và D1**, không phải câu hỏi sống/chết. 🔴 **ĐÍNH CHÍNH 2026-08-16 — chữ "cụt" đọc thành "bỏ được" là SAI, và hoà theo cách đó sẽ đánh rơi việc thật.** Đo lại bằng `git merge-tree --write-tree` (dạng CŨ 3-tham-số báo **0 xung đột** — nó trả giá trị trông hợp lệ đúng lúc không đo được gì): **239 đường / 109 thông điệp CONFLICT**, không phải 109 đường. Phân rã: **82/109 (75 %)** là hai cách sắp xếp KHÁC NHAU của cùng đám module chết dưới `Legacy/` (HEAD gom `Legacy/genmagic-v3.3/`, main rải `Legacy/stale-genmodel-2026-07/`) — không một byte mã sống. **27/109** là xung đột nội dung thật, **67 hunk**, và gần hết quy về MỘT nguyên nhân: hai bên hiện thực `INV-VAULT-IDENTITY` bằng hai kiến trúc **không tương thích nhị phân** (main: validator riêng `vault_id_nft.ak`, policy = hash validator đó, asset name cố định `#"564c54"`, apply-param vault **6** · HEAD: mint gộp trong vault, policy = script hash của chính vault, asset name `blake2b_256(cbor(seed))`, apply-param **4**) — xem **D12**. **Và vế ngược mà dòng cũ bỏ sót:** `origin/main` **đi TRƯỚC** HEAD ở ba chỗ — `coalesce_holdings` chặn `loyalty_holdings` phình tới trần `MAX_LOYALTY_HOLDINGS=64` (`lock.ak:84` trên main; `git grep -i coalesce` trên HEAD → **rỗng**), `unlockLockedAmount` đưa vào `ProtocolUtils` dùng chung (+69 dòng mã, +74 dòng test; HEAD tự viết bản riêng trong `ScheduleGen/offchain/src/math.ts`), và 5 commit `f36dd9b7..origin/main`. `coalesce_holdings` nằm TRONG hunk xung đột của `lock.ak` ⟹ hoà kiểu "lấy hết bên HEAD" là mất nó im lặng. 🔴 **Tuyệt đối không `git merge -s ours`** — nó nuốt cả 5 commit đó mà không sinh một dòng xung đột |
| 25 | `C-INST-2` "Max purchase `lamp_paid ≤ 10¹³`" được tài liệu hoá nhưng **không được cưỡng chế ở đâu** | `InstantGen/TECH.md:146` trỏ `vault.ak:148`, nhưng dòng đó là phần khai `spend(...)`, và `grep lamp_paid --include='*.ak'` chỉ ra **một** dòng — chú thích ở `lamp.ak:8` ghi biến đó ĐÃ XOÁ 2026-08-09. `grep "10_000_000_000_000"` trong `.ak` → rỗng. Cùng lớp với hai chú thích tự nhận là chốt an ninh đã vá ở `51a336f3`: tài liệu làm người đọc tin có chốt. Xoá dòng TECH.md, rồi chốt riêng xem trần mua có cần dựng lại. 🔴 **MỞ RỘNG 2026-08-17 — không phải một dòng hỏng, mà cả BẢNG hỏng.** Đo lại toàn bộ bảng invariant `InstantGen/TECH.md` theo `validate_instant_gen` (`vault.ak:350-498`): `lamp_paid` không sống sót ở bất cứ đâu trong `InstantGen/*.ak` ngoài đúng chú thích `lamp.ak:8`; **7/13 ID trong bảng không có mặt trong mã**; **mọi** neo `vault.ak:NNN` đều đã trôi; và bảng **thiếu bốn kiểm đang chạy thật** (epoch đơn điệu `:375`, depeg `:406`, beacon ôi `:410`, `claimed == grant` `:422`). Kèm hai câu SAI trong chính tài liệu, đã vá có ghi 🔴: hàm là `prune_expired` chứ không phải `halve_then_prune`, và câu *"`activity_state` không được liệt kê trong A02"* là **sai** — `vault.ak:481-482` kiểm nó và ép `consumed_credit` về 0 (đó đúng là chốt chống nhận-hai-lần, tài liệu bịa ra một lỗ ngay tại đó). Đã viết lại bảng từ mã đo, vá `MATH.md` §biên (bỏ 4 dòng `lamp_paid`, biên đặt trên **số dư**, và ghi rõ **không có trần trên**) và `EXEC.md` N1-N3 (N2 cũ kiểm một ràng buộc không tồn tại ⟹ thay bằng ca beacon ôi). Câu hỏi chính sách còn treo: trần mua có cần dựng lại không |
| 26 | 🔴 `GetMAGIC`: hai khuôn thông điệp **không đơn ánh** — nối trường độ-dài-thay-đổi rồi ký/băm, không đóng khung độ dài | Lớp lỗi Phoenix báo 2026-08-13 (họ vừa bắt cùng loại ở luồng đăng nhập của mình). Soát `GetMAGIC/` ra hai chỗ, và **`grep bytearray.length` trong cả module → rỗng**: không có một ràng buộc độ dài nào. (a) `utils.ak:21-31 build_oracle_settle_msg` = `order_id ∥ user_pkh ∥ nonce ∥ ts(8)` — **ba** trường đầu đều `ByteArray` tự do và đều do người dùng đặt (`order_id`/`user_pkh` trong datum, `nonce` trong redeemer, `otc_order.ak:62-68`). Chú thích khai `(16)(28)(32)` nhưng **không gì cưỡng chế**. Chữ ký ed25519 phủ đúng chuỗi nối ⟹ một chữ ký oracle cấp cho một cách tách vẫn hợp lệ cho mọi cách tách khác của cùng dãy byte; kẻ đặt đơn chọn được `order_id` nên nhét sẵn một pkh vào đó rồi tách lại để `user_pkh` thành khoá khác. (b) `types.ak:49 alloc_id = blake2b_256(order_id ∥ user_pkh)` — **hai** trường tự do, băm thành CHÍNH ĐỊNH DANH của allocation. (c) `build_voucher_msg` thì **an toàn**: chỉ một trường tự do ở đầu, 24 byte cố định phía sau nên tách được duy nhất. Chưa deploy mạng nào và module có **0 test Aiken** (Nợ #16) nên chưa mất tiền. Vá: đóng khung độ dài `u32` big-endian, **hoặc** ký theo mảng CBOR — và điều đó **trả lời luôn câu treo ở PR #21 điểm 3**: `oracle/` nhánh preserve ký theo mảng CBOR (tự đóng khung), `GetMAGIC` nối byte thô; chọn bản CBOR |
| 27 | 🔴 Ba gói `offchain` **không cài nổi từ checkout sạch**, và một gói **âm thầm mất 36 test** | Ba lớp lỗi khác nhau, cùng lộ ra ngày 2026-08-16 khi lần đầu có ai đó xoá `node_modules` rồi cài lại. (a) **Lockfile lệch `package.json`**: `GetMAGIC/offchain` và `Paymaster/offchain` cho `npm ci` chết ngay — `EUSAGE … lock file's @lucid-evolution/lucid@0.4.34 does not satisfy @lucid-evolution/lucid@0.4.30` (và `core-types@0.1.24` vs `0.1.22`). Tức lockfile đã track trong git **chưa từng** dựng lại được. (b) **Gói `libsodium-wrappers-sumo@0.7.16` đóng gói hỏng**: tệp ESM của nó `import` `./libsodium-sumo.mjs`, mà tệp đó nằm ở gói KHÁC (`libsodium-sumo`) ⟹ `ERR_MODULE_NOT_FOUND` lúc chạy. Hậu quả ở `ConsumeMAGIC/offchain`: vitest bỏ qua 3 tệp test và báo **31 passed** — xanh, không đỏ dòng nào, mà thiếu **36** ca. Vá: `"overrides": { "libsodium-wrappers-sumo": "0.7.10" }` ⟹ 67/67. (c) **`npm ci` với phụ thuộc `file:`**: npm chạy `prepare`/`build` của gói `file:` **ngay trong thư mục gói đó**, nơi chưa có `node_modules/.bin/tsc` ⟹ `sh: tsc: command not found`, exit 127. Không tài liệu nào ghi (đúng phát hiện (iii) ở bình luận PR #30). Đã mã hoá thứ tự tôpô vào `.github/workflows/pr-verify.yml`. **`UMKeeper` cố ý KHÔNG đụng**: nó khoá 0.7.16 nhưng `rm -rf node_modules && npm ci && vitest` cho 20/20 — test của nó không chạm đường import hỏng.<br>**Đã soát HẾT 2026-08-17** — `npm ci --dry-run` (kiểm lockfile ↔ `package.json`, không đụng `node_modules`) chạy trên cả **14** gói có lockfile trong kho: `AppEconomics/offchain`, `Consolidate/offchain`, `ConsumeMAGIC/offchain`, `ConsumeMAGIC/pricing`, `FlowRate/offchain`, `GetMAGIC/offchain`, `InstantGen/offchain`, `MagicSDK`, `Paymaster/offchain`, `ProfileChange/offchain`, `ProtocolUtils`, `ScheduleGen/offchain`, `UMKeeper/offchain`, `scripts` — **0 gói còn lệch**, không gói nào thiếu lockfile. Phần (a) đóng |
| 28 | `SPEC/MagicLamp-Tripletoken-Feat-(Vi).md` **phân đôi** — PR #23 và HEAD sửa cùng gốc theo hai hướng | Cùng xuất phát từ `585e05d7`. PR #23 sửa `+23/−23`; HEAD sửa `+45/−10`. Cả hai đều là bản "canonical" theo lời của chính mình, mà issue #26 và Nợ #24 đều trỏ về tên tệp này. Chưa hoà được vì chưa ai chốt bản nào là gốc — xem **D13**. Đây là lý do không nên gộp PR #23 bằng nút merge trên giao diện: nó sẽ tạo bản thứ ba.<br>**Đã đo hết 2026-08-17.** (1) **`585e05d7` là gốc về NỘI DUNG nhưng git không biết**: `git merge-base --is-ancestor 585e05d7 HEAD` → NO; gốc chung git thấy là `5d3c125e`, nơi **tệp chưa tồn tại**. HEAD có tệp vì `bd8c9404` (9/8) **chép nguyên khối vào** — cùng blob `72be462a` với `585e05d7`, mà thông điệp commit đó (`feat(pricing): gói định giá build ra ESM + CJS`) **không nhắc một chữ** về hai tệp `SPEC/` đi kèm. Đó chính là hành động tạo ra sự phân đôi, và **không có chỗ nào ghi lý do**. (2) **Bản chất hai bên khác loại, không mâu thuẫn**: HEAD = *đúng-với-mã* (2 hunk: `MagicBatch` 7→9 trường, `source: Int`→`BatchSource`, `Option<>` cho 2 trường, `EngageDatum` 4→5 trường, `C-CM-1..5`→`1..8`); PR #23 = *sạch-để-công-bố* (9 hunk, 447→447 dòng, tiêu đề mục **giống hệt** gốc, bỏ 23 chỗ kể tên chủ sở hữu/tên vai/số ghế — **không đổi một chữ số nào**, word-diff lọc theo chữ số xác nhận). Không bản nào có cả hai. (3) **Bản 3 tuỳ cách hoà**: bấm nút merge hôm nay → `git merge-tree --write-tree origin/main origin/docs/spec-tripletoken` EXIT=0, tệp kết quả là blob PR #23 **từng byte** (vì `main` chưa có `SPEC/` nào); hoà HEAD sau đó → **add/add, 574 dòng, 14 vùng xung đột**, lớn nhất là cả khối `MagicBatch` 9-trường chọi cả khối 7-trường; còn hoà theo ĐÚNG gốc nội dung (`git merge-file` với base `585e05d7`) → **1 xung đột duy nhất**, ở đúng đoạn `INV-VAULT-IDENTITY`, và hai vế là **hai cách viết cùng một câu**. Tức 14 vùng kia là do git mất gốc, không phải nội dung chọi nhau. (4) 🔴 **Bản sẽ nằm trên `main` nếu bấm nút là bản ghi SAI chỉ số constructor**: nó ghi `source: Int` "1=Instant 2=Schedule 3=Prepaid", mã là `BatchSource { Snapshot Instant Vacuum Schedule }` = 0/1/2/3 (`InstantGen/onchain/lib/magiclamp/protocol/types.ak`) ⟹ `Schedule` sai (2 vs 3) và `Prepaid` **không tồn tại** trong enum; cộng thiếu 2 trường `MagicBatch`, 1 trường `EngageDatum` — đúng ba thứ `BOUNDARIES.md §2` xếp vào "vi phạm là hỏng tiền thật". (5) **Một vết trên chính HEAD**: HEAD viết `halved` "Mọi giá trị `True` bị từ chối"; mã tự đính chính 2026-08-12 ngay trong `types.ak` rằng **không có `expect` nào** từ chối `halved == True` — HEAD đúng hình dạng, sai một câu về cưỡng chế. (6) Tuân đã nêu đúng lỗi 7-vs-9 từ 31/7 trong chính PR #23 (mục D) và xếp vào "sửa spec là xong" — nhóm anh ấy coi là chặn (A/B/C) nằm chỗ khác |
| 29 | `PrepaidGen` khai `CARPDROP_PER_CARP = 1_000_000n` (6 chữ số) trong khi quyết định 2026-08-06 chốt CARP **9 chữ số** (`nanothread`, par = ×1) | `PrepaidGen/offchain/src/constants.ts:7` trong cây stash (tag `preserve/prepaidgen-stash-2026-07-30`, xem Nợ #5). Lệch **10³** — mã viết trước quyết định, chưa ai rà lại. Chưa hỏng gì vì cây đó chưa vào cây làm việc; nhưng phải vá TRƯỚC khi `pop`, không thì nó vào kèm lỗi đơn vị ngay từ commit đầu |
| 30 | ✅ **ĐÃ VÁ 2026-08-17** — ~~`loyalty_holdings` chỉ tách, không bao giờ gộp ⟹ vault ScheduleGen TỰ KHOÁ CHẾT sau ~62 lần bắn, LAMP đã khoá đóng băng vĩnh viễn~~ | **Cách vá: bê nguyên bản của `origin/main`, không viết lại** (nó đã có test hồi quy và đã sống lâu hơn). Đã thêm `coalesce_holdings` + `same_bucket` + `merge_into` vào `ScheduleGen/onchain/lib/magiclamp/protocol/lock.ak:68-113`, gọi ở cuối `unlock_locked_amount` (`:65`); bản TS gương `coalesceHoldings` ở `ScheduleGen/offchain/src/math.ts:71-84`, gọi ở `:62` — **cùng commit** (P8). Bốn test mới, hai bên đối xứng: Aiken `ul_repeated_does_not_grow` + `ul_keeps_distinct_epochs_apart` (`vault.ak:1448-1470`), TS hai ca cùng tên việc (`ScheduleGen/tests/schedule.test.ts:497-527`). Ca thứ hai chặn cái bẫy ngược: `acquired_epoch` **là** tuổi trung thành, gộp nhầm hai epoch = xoá tuổi. **Số đo sau khi vá**: `aiken check` **53/53 xanh, 0 lỗi, 0 cảnh báo**; `vitest` **43/43** (trước 41); `tsc --noEmit` exit 0; `aiken build` 0 lỗi và `vault.vault.spend` **11.210 → 11.421 byte (+211, +1,88%)**, `vault.shard.spend` không đổi. Còn lại **chưa vá, cố ý**: `validate_commit` (`vault.ak:245`) vẫn không ép trần ≤64 như `validate_fire` (`:460`) — đường sinh sôi đã bịt nên không còn là chặn mainnet, nhưng vẫn nên siết cho đối xứng. **Bối cảnh gốc** (giữ lại để không quên vì sao): Agent A4 nêu 2026-08-17; **bên này kiểm lại từng neo, đúng hết**. Chuỗi: `lock_youngest` tách một holding thành 2 khi khoá một phần (`ScheduleGen/onchain/lib/magiclamp/protocol/lock.ak:34-38`); `unlock_oldest` cũng tách thành `freed` + `still_locked` khi giải phóng một phần (`lock.ak:88-100`) ⟹ **mỗi `ScheduleFire` cộng đúng 1 phần tử**. `grep -rni coalesce --include='*.ak' --include='*.ts' .` (trừ `node_modules`, `Legacy`) → **0 dòng**: không có hàm gộp nào, cả on-chain lẫn off-chain. `validate_fire` ép `list.length(output.loyalty_holdings) <= max_loyalty_holdings` (`validators/vault.ak:460`, hằng = 64 ở `constants.ak:43`) — nhưng **`validate_commit` KHÔNG ép** (đo: trích thân `validate_commit` grep `loyalty_holdings` → chỉ `:90/:97/:111`, không có kiểm độ dài) ⟹ commit đẩy được danh sách vượt 64, và từ đó fire chết vĩnh viễn. LAMP đã khoá **chỉ** ra được qua fire: `validate_withdraw_lamp` chặn ở `l_avail = balance − locked` (`vault.ak:962-963`), và chính nó cũng ép ≤64 trên output (`:999`). `Consolidate/` **không cứu được** — README của chính nó khai script hash RIÊNG nên UTxO ở địa chỉ vault không bao giờ chạy validator đó (`Consolidate/README.md:8-11`). `VaultRedeemer` không có nhánh gộp (`vault.ak:153-177`). **Số học**: `schedule_max_length = 200`, bắn đều 1 fire/tx ⟹ chết quanh fire thứ **62**; lịch `L = 200` **không bao giờ** chạy hết. Hậu quả: `lamp_per_epoch × (L − đã_bắn)` đóng băng vĩnh viễn + vault ngừng sinh MAGIC. **Vì sao chưa lộ**: 13 vault testnet chưa vault nào bắn tới ~60 lần. 🔴 Đây là **chặn mainnet**, và nó lật đánh giá cũ ở D12 mục (5) — `coalesce_holdings` của `origin/main` **không phải lợi thế phụ, nó là bản vá cho đúng lỗi này**. **ĐÃ ĐO xong phía main 2026-08-17** (A4 không có Bash nên để `[CHƯA ĐO]`; bên này chạy `git show`): main vá ở **cả hai tầng, và biết rõ mình đang vá gì**. On-chain: `coalesce_holdings` ở `origin/main:ScheduleGen/…/lock.ak:84-102` (gộp theo cặp `(acquired_epoch, is_locked)` — `same_bucket:88-90`), **gọi ngay trong** `unlock_locked_amount` (`:67`), tức vá đúng đường sinh sôi (mỗi lần bắn +1). Off-chain: `origin/main:ProtocolUtils/src/index.ts:222` trả `coalesceHoldings([...unlocked, ...freed, ...stillLocked])`, chú thích `:228` viết thẳng *"NOT optional"*. Ba test hồi quy ở `origin/main:ScheduleGen/onchain/validators/vault.ak:1287-1315`, trong đó `f_unlock_repeated_does_not_grow` ghim `list.length == 2` sau 3 lần bắn, và `f_unlock_keeps_distinct_epochs_apart` chặn cái bẫy ngược lại (gộp nhầm hai epoch khác nhau là **xoá tuổi trung thành**). 🔴 Chú thích `vault.ak:1296-1299` của main mô tả **đúng** lỗi này trước cả A4: *"21 entries after 20 fires, ~201 after an L=200 schedule, against max_loyalty_holdings=64 … would freeze the user's LAMP outright"*. ⟹ đây không phải lỗi chung của hai nhánh: main **đã vá và có test**, HEAD **đánh mất bản vá** khi tách nhánh. P8 vẫn nguyên ở cả hai bên — HEAD `ScheduleGen/offchain/src/math.ts:60` trả `[...unlocked, ...freed, ...stillLocked]` không gộp, **khớp bit** với Aiken hỏng của chính nó; và HEAD `ProtocolUtils/src/index.ts` **không có** `unlockLockedAmount` lẫn `coalesceHoldings` (`grep` → 0 dòng). Cách vá cho HEAD: bê nguyên `coalesce_holdings` + 3 test + bản TS, KHÔNG viết lại |
| 31 | ✅ **ĐÃ VÁ 2026-08-17** — ~~Đếm-thiếu `shard_locked_lamp`: hai vault đồng-chủ khác `stake_credential` cùng ghi một delta lên MỘT shard ⟹ vượt `SHARD_CAP` tuỳ ý, và số đếm xuống ÂM~~ | Agent A4 nêu 2026-08-17; **bên này kiểm lại từng mắt xích, đúng hết**. (a) Đúc được vault thứ hai cùng script hash nhưng khác địa chỉ: `validate_mint_vault_id` chỉ đòi `o.address.payment_credential == Script(policy_id)` (`ScheduleGen/onchain/validators/vault.ak:859`) — **`stake_credential` hoàn toàn không bị ràng**. (b) Chốt chống double-satisfaction lại so **`Address` ĐẦY ĐỦ**: `expect list.count(tx.inputs, fn(i) { i.output.address == vault_addr }) == 1` (`:151`, C-VAULT-DS-1) với `vault_addr = own_addr(own_ref, ...)` ⟹ V1 (`stake=None`) và V2 (`stake=Some`) mỗi bên tự đếm được 1, **cả hai qua**. (c) Phía shard không cản: `SHARD-COSPEND` dùng `list.any` — chỉ cần **tồn tại một** vault khớp (`vault_cospend_matches`, `:1061-1080`), không ràng buộc **số** vault input; `SHARD-DS-1` chỉ đếm shard input, `SHARD-OUT-1` chỉ đếm shard output (`:208`, `:212-216`). Hai vault cùng `L·λ` khẳng định cùng một delta trên **một** output shard ⟹ cả hai thoả. Hậu quả: khoá thật `2·L·λ` nhưng sổ ghi `L·λ`; nhân K vault/tx là thiếu K lần ⟹ `SHARD_CAP = 4,5×10¹⁴ oildrop` (`constants.ak:50`) — trần tham gia hiến định — vượt tuỳ ý. Tệ hơn: khi các vault fire ở **các tx riêng**, mỗi tx trừ shard một lần ⟹ `shard_locked_lamp` **xuống âm** ⟹ headroom nới **vĩnh viễn** cho mọi người rơi vào shard đó. 🔴 **Kho này ĐÃ BIẾT lỗi và vá ở hai module khác, chỉ quên đúng chỗ vault sống**: `Consolidate/onchain/validators/vault_consolidate.ak:19-20` ghi nguyên văn *"Đếm input/output theo PAYMENT CREDENTIAL (script hash) → chống double-satisfaction qua stake credential"*, và `ConsumeMAGIC/onchain/lib/magiclamp/consume/util.ak:67-72` dùng `is_at_script` theo payment credential. Một kho, hai định nghĩa "ở địa chỉ của tôi". **Footgun không cần ác ý**: ai gọi `credentialToAddress(NETWORK, cred, stakeCred)` để vault stake ADA sẽ vô tình tách sổ shard. Vá: đổi `:151`, `:208`, `:779-780`, `:787`, `:1067` sang so payment credential. **Bổ sung 2026-08-17 (A5, bên này tự kiểm hai bên): KHÔNG có lưới thứ hai ở bản nào.** Nhánh fire chỉ ghi `expect shard_out.shard_locked_lamp == shard_in.shard_locked_lamp - lamp_released` — **thiếu `expect shard_out.shard_locked_lamp >= 0`** — ở `ScheduleGen/onchain/validators/vault.ak:442` (HEAD) **và** `origin/main:…/vault.ak:374`. Nhánh commit có trần trên `<= shard_in.shard_cap` (`:307-308` HEAD / `:251-252` main) nhưng không có sàn dưới. Thêm một dòng `>= 0` hạ thiệt hại từ "nới headroom **vĩnh viễn** cho mọi người rơi vào shard đó" xuống "tx bị từ chối" ⟹ **vá kèm trong cùng commit với phép đổi payment credential**.<br>**ĐÃ VÁ 2026-08-17 — số đo thật, không phải ước lượng.** Hình dạng bản vá: tách **ĐẾM** khỏi **ĐỊNH DANH**. Thêm `fn same_script(a, b) { a.payment_credential == b.payment_credential }` rồi đổi mọi phép **đếm** input/output sang nó, đồng thời **siết** định danh: output tiếp diễn phải mang **đúng `Address` đầy đủ của input** (`expect o.address == vault_addr`). Nếu chỉ nới đếm mà không ghim định danh thì `ScheduleFire` — nhánh **permissionless** — cho người lạ đổi stake credential của vault, tức chuyển hướng phần thưởng stake của chủ vault. Cũ chặt sai chỗ, mới chặt đúng chỗ, không chỗ nào lỏng ra.<br>**Phạm vi rộng hơn dòng trên ghi: `InstantGen` mang y hệt lỗi, 8 điểm.** `InstantGen/onchain/validators/vault.ak:360, 526, 753-754, 794, 878, 1018, 1072` cùng khuôn `i.output.address == vault_addr`, và chú thích ở `:356-357` còn khai sai nguyên do — *"Address match alone is sufficient — each vault UTxO has a unique own_ref"* — lẫn giữa **own_ref** (đúng là duy nhất) với **address** (không duy nhất, vì nửa stake không bị ràng). Đã vá cùng lượt và sửa luôn chú thích; **4 vault InstantGen đang giữ tLAMP trên hai testnet** nên để lại là để một khuôn sai trong validator sống. `Consolidate` và `ConsumeMAGIC` đã đúng từ trước, không đụng.<br>**Điểm sửa ScheduleGen:** `vault.ak:151` (C-VAULT-DS-1), `:208` (SHARD-DS-1), `:812-813` (C-VAULT-OUT-1, kèm ghim `:817`), `:797` (`find_vault_input_raw`), `:1074` (`vault_cospend_matches` loại input shard). SHARD-OUT-1 **không đụng** — nó vốn đã đếm theo NFT chứ không theo địa chỉ.<br>**U5 vá kèm:** thêm `expect shard_out.shard_locked_lamp >= 0` ở nhánh fire (`vault.ak:445-450`).<br>**Kiểm chứng bằng mutation test** (kỷ luật A4 nói main có mà HEAD thiếu): bản sao scratchpad, hoàn nguyên đúng hai dòng đếm output về `x.address == vault_addr` ⟹ `ds_phantom_output_under_stake_cred_rejected` **chuyển FAIL** (`Summary 55 checks, 1 error`), tức lỗ **có thật**, không phải giả định. Test thứ hai `ds_continuing_output_must_keep_stake_cred` **rớt ở cả hai bản** nhưng ở hai chốt khác nhau — nó là **lưới chống nới lỏng về sau**, không phải bằng chứng lỗ cũ; ghi rõ để không tính nhầm công. Cả bốn test mới đều đã đọc dòng trace `x <expected>` để chắc rớt **đúng chốt định rớt** (bẫy `placeholder` validity_range vô hạn A5 nêu).<br>**Số đo sau vá:** `aiken check` ScheduleGen **55 checks, 0 errors, 0 warnings** (47 test, trước 45); InstantGen **85 checks, 0 errors, 0 warnings** (82 test, trước 80). `aiken build` xanh cả hai. Kích thước: ScheduleGen `vault.spend` 11.421 → **11.479 B** (+58, +0,51%), `shard.spend` 5.131 → **5.152 B** (+21); InstantGen `vault.spend` 10.787 → **10.826 B** (+39, +0,36%). `vitest` ScheduleGen 43/43, InstantGen 55/55; `tsc --noEmit` exit 0.<br>**Off-chain không phải sửa, đã kiểm:** mọi nơi dựng địa chỉ vault đều gọi `credentialToAddress(network, cred)` **không truyền tham số stake** (`ScheduleGen/offchain/src/schedule.ts:215-216, 376-377`, `InstantGen/offchain/src/instant.ts:247`, `scripts/test/*.ts`, `scripts/deploy/03_deploy_shards.ts:81`) ⟹ 13 vault đang sống không có stake credential, bản vá **tương thích ngược** với chúng.<br>🔴 **Còn thiếu, cố ý ghi ra:** dòng `shard_locked_lamp >= 0` **chưa có test đơn vị** — nhánh `validate_fire` chết ở kiểm NFT trước khi tới sổ shard nên trong kho chưa có bộ đồ dựng shard đầy đủ; test `validate_fire` duy nhất (`vault.ak:1783`) là ca PoC không-NFT. Muốn phủ thì phải dựng fixture shard hoàn chỉnh — việc riêng, chưa làm |
| 32 | 🔴 **`base_price` không có TRẦN TRÊN — committee đặt giá tuỳ ý, người dùng không cản được. Có ở CẢ HAI nhánh** | Agent A5 nêu 2026-08-17; bên này tự kiểm cả hai phía P8, đúng. Aiken: `ConsumeMAGIC/onchain/lib/magiclamp/consume/pricing.ak:108-140` — mỗi dòng giá chỉ bị ràng `op.base_price >= 0` và `op.base_price * pp.m_min >= q`; **cả hai đều là ràng buộc DƯỚI**, cái sau chỉ chặn giá làm tròn về 0 (drain miễn phí). TypeScript đối chiếu đúng từng chữ: `ConsumeMAGIC/pricing/src/price.ts:288` `if (row.base_price * pp.m_min < Q)` và chú thích `:242-245` khai đúng ý định *"GATE này BAO HÀM `base_price ≥ 0` và cấm luôn `base_price == 0`"* — không có dòng nào nói tới cận trên. ⟹ một `PostPrice` hợp lệ đặt `base_price = 2⁶³` cho `op_type=7 (did.rotate)` là **khoá quyền xoay khoá của mọi người**, mà đó đúng thao tác tự vệ khi nghi lộ khoá (cùng bề mặt với D9/D10). HEAD đã ghim `m_min`/`m_max` về hằng giao thức (Δ4) nên dải NHÂN bị khoá, nhưng dải CỘNG thì không — ghim một nửa. Vá: thêm hằng `MAX_BASE_PRICE` vào `constants.ak` + `constants.ts`, `expect op.base_price <= max_base_price` trong `valid_param`, và **cùng commit** dựng lại điều kiện tương ứng ở `price.ts` (P8). Chưa làm vì phải chốt con số trần trước — đó là tham số kinh tế, không phải hằng kỹ thuật |
| 33 | **Datum khởi tạo shard không ràng người deploy — deployer đặt được `shard_cap`/`shard_locked_lamp` tuỳ ý cho 1/16 người dùng. Có ở CẢ HAI nhánh** | Agent A5 nêu 2026-08-17; tự kiểm: `git diff origin/main HEAD -- ScheduleGen/onchain/validators/shard_nft.ak` **rỗng** ⟹ hai nhánh giống hệt nhau từng byte, đây không phải trục phân kỳ D12. Thân mint chỉ có ba `expect`: tiêu đúng `genesis_ref`, `dict.size(own_tokens) == shard_count`, `all_shard_names_minted(...)` — **không dòng nào chạm `tx.outputs`** ⟹ 16 NFT đúc đúng số nhưng datum đi kèm là tuỳ ý người chạy `03_deploy_shards.ts`. Đặt `shard_cap = 0` cho một shard là **khoá vĩnh viễn** mọi DID băm vào shard đó (`compute_shard_id` = byte đầu blake2b(owner) % 16, người dùng không chọn được shard của mình); đặt `shard_locked_lamp` khởi tạo âm là nới headroom sẵn. Chưa vá vì phải quyết hình dạng: ép datum trong chính nhánh mint (đọc `tx.outputs`, đòi đủ 16 output đúng `shard_cap` hằng số) hay chấp nhận và đưa "kiểm 16 datum shard sau deploy" thành bước nghiệm thu bắt buộc. **Ảnh hưởng thực địa: 4 shard-set đã deploy trên 2 testnet chưa ai đối chiếu datum** |
| 34 | **`oracle_vkey` của GetMAGIC là TRƯỜNG CỦA CHÍNH DATUM nó xác minh — người tạo allocation tự ký voucher cho mình** | Agent A5 nêu 2026-08-17; tự kiểm: `GetMAGIC/onchain/lib/getmagic/types.ak:63` và `:89` khai `oracle_vkey: ByteArray` là trường của datum, còn `GetMAGIC/onchain/validators/magic_allocation.ak:89` `crypto.verify_ed25519_signature(datum.oracle_vkey, voucher_msg, voucher)` xác minh **ngược lại chính trường đó**; `otc_order.ak:69` cùng khuôn. ⟹ chữ ký oracle không chứng minh gì ngoài "người đặt datum biết khoá riêng của khoá công họ tự khai". **Mã ĐÃ TỰ KHAI lỗ này**: `magic_allocation.ak:21-26` ghi `KL-2 (oracle_vkey trust-root, CRITICAL@Phase-2)` kèm đúng cách vá — *"make oracle_vkey a VALIDATOR apply-param"*. Nên đây là nợ **đã biết, chưa xử**, không phải phát hiện mới; giá trị A5 thêm vào là xếp nó cùng bảng với Δ1–Δ6 để thấy nó cùng LOẠI với Δ4 (tự khai dải giá): trust-root nằm trong dữ liệu thay vì trong mã. Chặn: `magic_allocation.ak:120` đã ghim `updated.oracle_vkey == datum.oracle_vkey` nên không đổi được giữa chừng — vô nghĩa nếu giá trị ban đầu đã do người đó đặt. Vá phụ thuộc D11 (GetMAGIC còn ở lại repo không) |
| 35 | ✅ **ĐÃ VÁ 2026-08-27** (`07970bd1`) — `find_shard_output_by_policy` nay nhận địa chỉ mong đợi và đòi `o.address == expect_addr`; ba nhánh gọi (shard spend · ScheduleCommit · ScheduleFire) ghim theo địa chỉ của chính shard INPUT, đủ cả stake credential. 3 test mới; kiểm đột biến bỏ đúng dòng đó → 2 test đỏ, trả lại → 58 checks 0 errors. Off-chain không phải sửa (`schedule.ts:231,396` đã trả về đúng `shardAddr`). ~~**Không dòng nào kiểm ĐỊA CHỈ của output UTxO tổng-hợp shard — chỉ kiểm NFT.** Ai làm được một `ScheduleCommit` hợp lệ thì trong CÙNG tx đó gửi 16 NFT shard sang ví riêng, sau đó tự ghi `shard_locked_lamp` ⇒ `C-SCH-CAP` mất hiệu lực. Có ở **cả hai nhánh**. Neo: `find_shard_output_by_policy` trong `ScheduleGen/onchain`. Kiểm: đã dựng test `probe_shard_moved_to_wallet` trong bản sao scratchpad, chạy trên aiken v1.1.21 → **PASS** (tấn công thành công, không validator nào chặn)~~ |
| 36 | ✅ **ĐÃ VÁ 2026-08-27** (`8681cdcb`) — thêm `expect list.has(tx.extra_signatories, in_datum.owner)` vào nhánh spend. Off-chain theo cùng lượt: `buildConsumeTx` luôn thêm signer lấy từ datum đang tiêu, `consume_only.ts` cùng quy tắc. 2 test mới; kiểm đột biến → 2 đỏ; trả lại → 100 checks 0 errors. ~~**Nhánh SPEND của `consume` không đòi chữ ký `owner` của thread Engage.** `rg extra_signatories ConsumeMAGIC/onchain/validators/consume.ak` chỉ ra **một** dòng — `:253`, nằm trong nhánh MINT. `enforce_engagement` (`consume.ak:553-580`) chỉ ép owner **bảo toàn**, không ép owner **ký**. Xác thực duy nhất trong tx là phía vault (`ScheduleGen/onchain/validators/vault.ak:517-521`) ⇒ bất kỳ ai có MAGIC trong vault mình đều ghi được `consumed_count`/`consumed_nanogic` vào thread Engage của **người khác**. Hệ quả: C1 của công thức quyền biểu quyết mua được bằng tiền — đúng thứ hệ cấm~~ |
| 37 | **Giải mã Plutus Data NGHIÊM NGẶT VỀ SỐ TRƯỜNG, cả hai chiều — nên "thêm trường ở cuối" KHÔNG giữ được UTxO đã tạo.** Chạy thử trên aiken v1.1.21: datum 2 trường đọc bằng type 3 trường → FAIL; datum 3 trường đọc bằng type 2 trường → cũng FAIL; cả hai chết đúng dòng `expect n: NewD = d`. Chú thích `ConsumeMAGIC/onchain/lib/magiclamp/consume/types.ak:51` phát biểu **đúng** phạm vi ("THÊM Ở CUỐI để KHÔNG dịch chỉ số field cũ") — nợ này không phải để sửa nó, mà để ghim điều nó **không** hứa: thêm trường ở cuối giữ được *chỉ số* trường cũ, nhưng mọi UTxO đã tạo theo hình dạng cũ **hết decode được**. Trên Cardano, thứ đóng vai "version" của một datum không phải số trường — mà là **địa chỉ script**. Đổi hình dạng datum = một script hash mới = một kho mới, và kho cũ phải được rút cạn bằng chính mã cũ trước đó |
| 38 | **Mã `C-CM-6` chỉ hai ràng buộc KHÁC NHAU trên hai nhánh.** `origin/main:ConsumeMAGIC/CONTRACT.md:85` = `INV-VAULT-IDENTITY` (mỗi vault input mang đúng 1 NFT one-shot); `HEAD:ConsumeMAGIC/CONTRACT.md:189` = bất biến kế toán `consumed_nanogic`. Bảng §7.4 của spec **cả hai bản** chỉ liệt tới C-CM-5 nên không phân giải được ⇒ hoà nhánh theo tên mã sẽ **im lặng đánh rơi một ràng buộc** |
| 39 | 🔴 **`ActivityState` trường thứ hai mang hai nghĩa khác nhau trên cùng một ô byte.** `origin/main:ScheduleGen/onchain/lib/magiclamp/protocol/types.ak:99` = `total_burns_count : Natural` (ĐẾM lượt); `HEAD:…:108` = `consumed_credit : Natural` (nanogic đã tiêu, chưa quy thưởng). Cùng vị trí, cùng kiểu, **cùng hình dạng Plutus Data** ⇒ decode không bao giờ báo lỗi. Off-chain viết theo main ghi số ĐẾM vào ô mà InstantGen của HEAD đọc là **ngân sách thưởng**. Spec im lặng hoàn toàn về `ActivityState` ở cả hai bản |
| 40 | 🔴 **`origin/main:ConsumeMAGIC/onchain/validators/price_param.ak` thiếu ba dòng bảo vệ** mà HEAD có ở `:54-56` (`expect threshold > 0` · `expect threshold <= list.length(committee)` · `expect util.all_distinct(committee)`). main chỉ có `:37 expect count_sigs(committee, tx.extra_signatories) >= threshold`. `threshold` là apply-param do người deploy đặt ⇒ đặt 0 thì `>= 0` luôn đúng, **ai cũng post được beacon giá**; committee trùng khoá (`[c1,c1,c2]`, threshold=2) thì một mình `c1` ký đủ. Beacon này là reference input của **mọi** tx ConsumeMAGIC |
| 41 | ✅ **ĐÃ VÁ 2026-08-27** (`7a64fb0c`) — ghim ba vế: địa chỉ của chính input đang tiêu (dùng `self_ref`, tham số trước đó bỏ không), ĐÚNG MỘT output ở địa chỉ đó, và giá trị bảo toàn. ~~**`continuing_output_ok` của GetMAGIC không kiểm ĐỊA CHỈ output — cùng lớp với Nợ #35.** `GetMAGIC/onchain/validators/magic_allocation.ak:110-128` duyệt `list.any(tx.outputs, …)` và chỉ so NỘI DUNG datum; không một lần hỏi `o.address`. ⇒ nhánh ClaimEpoch cho phép bản ghi phân bổ (kèm toàn bộ giá trị UTxO) đi thẳng tới ví trần, validator vẫn qua. Đây là lớp lỗi thứ hai trong repo có cùng hình dạng "kiểm nội dung, quên địa chỉ" — nên coi là lỗi HỆ THỐNG, không phải lỗi lẻ~~ |
| 42 | ✅ **ĐÃ VÁ 2026-08-27** (`7a64fb0c`) — ràng output theo `order_id` của chính đơn (`count_alloc_outputs_for_order(...) == 1`), gộp lô vẫn hợp lệ khi mỗi đơn có output riêng. 5 test mới; kiểm đột biến bỏ ràng theo đơn → 3 đỏ. Hai hệ quả nhỏ cùng gốc: "output là bụi không datum" **đã đóng** (nay đòi inline `AllocationDatum`); **CÒN MỞ** vế stake credential — phép kiểm dùng `payment_credential` nên phần thưởng uỷ quyền vẫn chảy đi nơi khác được. Chưa siết vì `AllocationDatum` có trường `beneficiary_stake`, tức địa chỉ allocation có thể mang stake theo thiết kế; ép địa chỉ script trần là quyết định thiết kế, không phải vá lỗi. Vẫn nằm trong KL-2: chưa ràng nội dung các trường còn lại. ~~**`alloc_output_exists` của GetMAGIC cho phép hai đơn Settle dùng CHUNG một output.** `GetMAGIC/onchain/validators/otc_order.ak:91-95` chỉ hỏi `list.any(tx.outputs, o.address.payment_credential == Script(alloc_script_hash))` — không gắn với `order_id` của chính input, không ĐẾM số output, không đòi output mang datum nào (chú thích `:89` tự khai "Off-chain is responsible for embedding the correct AllocationDatum", tức đẩy một bất biến tiền bạc sang phía off-chain). ⇒ một tx gồm hai input Settle của hai khách đã trả tiền thật, kèm ĐÚNG MỘT output allocation, thì cả hai input đều qua độc lập — một khách mất trắng. Kèm hai hệ quả nhỏ hơn cùng gốc: output có thể là bụi không datum, và stake credential không bị ràng nên phần thưởng uỷ quyền chảy đi nơi khác~~ |

## Chờ chủ nhân chốt

| # | Câu hỏi | Vì sao không tự quyết |
|---|---|---|
| D1 | Viết lại trần InstantGen theo SPEC §6.3 + hiện thực `INV-INSTANT-LOCK`? | là viết lại công thức sinh MAGIC + thêm trường datum, không phải sửa lỗi. §6.2 (tư-cách 4 thành phần) cũng chưa từng tồn tại trong code. **2026-08-12: không còn là câu hỏi kiến trúc thuần — Nợ #19 chứng minh InstantGen hiện KHÔNG chạy được trên mạng nào.** Ba hình dạng vá, chọn một: (a) hợp nhất hai validator thành MỘT script mang cả `ScheduleCommit` lẫn `InstantGen` (đúng ý PHA-2, nhưng đổi mọi hash vault, deploy lại từ đầu); (b) cho vault InstantGen ĐỌC vault ScheduleGen của cùng chủ qua reference input rồi lấy `gen_schedules` ở đó (không đổi datum, nhưng thêm apply-param + phải chống trỏ nhầm vault người khác); (c) bỏ `cap_pp` khỏi `min3` và thay bằng trần khác. (c) là bỏ trần-kép §6.3 nên bên này KHÔNG đề xuất. **2026-08-13 — soát lại thu hẹp còn MỘT lựa chọn.** Nợ #19 nay chỉ ra hai khoá, không phải một: `cap_pp = 0` VÀ `consumed_credit = 0`. (b) và (c) chỉ gỡ khoá thứ nhất; khoá thứ hai đòi vault InstantGen phải có MAGIC để tiêu trước, mà nguồn MAGIC sống duy nhất là `ScheduleFire` — nhánh ở validator kia. ⟹ chỉ (a) hợp nhất một script mới mở được cả hai. **🔴 ĐÍNH CHÍNH cùng ngày, sau khi đối chiếu SPEC: câu trên SAI cho khoá A.** `SPEC:222/229-230/241` đã **bỏ hẳn** `gen_schedules` khỏi trần thứ ba và định nghĩa lại là `⌊L_avail × RATE_REF_Q / Q⌋` (`RATE_REF_Q = 10¹²`, `L_avail = lamp_balance − lamp_locked`), kèm câu "Vì sao bỏ hệ-số `0.5×` cũ". Mã thì vẫn là bản v3.3 (`math.ak:119-130` cộng theo `gen_schedules` rồi chia 2) và `RATE_REF_Q` **không xuất hiện một lần nào** trong `InstantGen/` hay `ScheduleGen/`. ⟹ khoá A là **mã lệch spec**, gỡ được bằng thay đổi **thuần toán trong chính InstantGen** (dữ liệu `lamp_balance`/`lamp_locked` có sẵn trong datum của nó), không cần đụng ScheduleGen, không cần hợp nhất. Phải làm kèm `INV-INSTANT-LOCK` (`SPEC:233/235`) — nếu không là mở đường flash-rent LAMP. Khoá B thì vẫn cần nguồn `consumed` bên ngoài, và có **đường (e)** chưa từng cân nhắc: đọc `EngageDatum.consumed_nanogic` (`ConsumeMAGIC/…/types.ak:60-66`, đã có bất biến cưỡng chế) qua reference input, chống-tiêu-hai-lần bằng mốc nước cao `last_rewarded_nanogic` trong datum vault thay vì zero `consumed_credit`. Đường (e) **đúng ngữ nghĩa spec** (`SPEC:389` đòi nguồn engage-side cross-DID, KHÔNG đọc tổng-sự-kiện-vault vì số đó self-burn bơm được) và vẫn đổi hash vault, nhưng việc 3 của issue #26 đằng nào cũng thêm trường datum nên chi phí đó đã nằm trong phạm vi. Ba dữ kiện làm (a) rẻ **ngay lúc này** — vẫn đúng nếu chọn hợp nhất: `VaultDatum` hai bên **trùng từng trường, từng thứ tự** (17 trường, đã đối chiếu), bảng dispatch hai bên đều là một `when redeemer is` phẳng (`InstantGen/…/vault.ak:158`, `ScheduleGen/…/vault.ak:153`), và **chưa vault MAGIC nào trên mainnet** nên đổi hash không phải migrate ai. Sau mainnet thì mỗi vault đang sống phải chuyển tay. Việc phải làm kèm: hợp nhất `VaultRedeemer` thành MỘT enum — hiện `BurnBatch` trùng chỉ số 2 ở cả hai (may), còn `WithdrawLamp`/`SetDelegate` lệch (4/5 ở Instant, 3/4 ở Schedule) nên union buộc đánh số lại |
| D11 | `Paymaster/` và `GetMAGIC/` còn nằm trong repo không? | Chủ nhân nêu 2026-08-12. Đo được: `GetMAGIC` **không xuất hiện một lần nào** trong `SPEC/MagicLamp-Tripletoken-Feat-(Vi).md`, và spec chốt đúng ba cửa sinh (§6: Instant · Schedule · Prepaid) — cửa fiat→MAGIC là cửa thứ tư, sinh MAGIC không có LAMP/CARP đứng sau. `Paymaster` thì spec CÓ giữ (§7.6) nhưng giữ dưới dạng `personal_delegate` + `BurnBatch` **của chính vault InstantGen** — cơ chế đó đã chạy, không cần module. Module `Paymaster/` là lớp SponsorMeter riêng, và nó còn mang `burn_batch_constr` chú thích "Snapshot=1, Vacuum=4" — hai mô hình đã bỏ. Xoá hai module là xoá ~67 vitest + 28 aiken check nên bên này không tự quyết |
| D12 | 🔴 `INV-VAULT-IDENTITY` hiện có **hai kiến trúc không tương thích nhị phân** — lấy bên nào? | Nguyên nhân gốc của gần hết 27 xung đột nội dung thật ở Nợ #24: chốt cái này là gỡ được phần lớn bức tường 239 đường. Hai bản, đo bằng lệnh chứ không nhớ:<br>· **`origin/main`** (PR #27, đã merge) — hai validator RIÊNG `ConsumeMAGIC/onchain/validators/vault_id_nft.ak` và `engage_nft.ak`, mỗi cái apply-param bởi một `genesis_ref: OutputReference`; asset name **cố định** `#"564c54"` (`"VLT"`, khai ở `vault_id_nft.ak:31`); `consume.ak` nhận **10** apply-param, trong đó có `vault_nft_policy/name` + `engage_nft_policy/name` (`git show origin/main:…/consume.ak`, dòng 42-54).<br>· **`HEAD`** — không có hai tệp đó (`ls ConsumeMAGIC/onchain/validators/` → `consume.ak price_nft.ak price_param.ak`); mint gộp vào chính script, cùng khuôn `validate_mint_vault_id` của 4 vault gen (`consume.ak:207-224`); asset name = `blake2b_256(cbor.serialise(seed))`; `consume.ak` nhận **7** apply-param, thêm `price_param_script_hash`.<br>**Khác biệt quyết định là bao nhiêu vault dùng chung một script đã biên dịch.** Bản main tự khai ngay trong chú thích: *"MVP là MỘT policy / MỘT vault — mỗi vault deploy một `genesis_ref` riêng nên policy id khác nhau"* (`vault_id_nft.ak:14-16`). Mà `consume.ak` lại ăn policy id đó làm apply-param ⟹ **mỗi vault đẻ ra một bản `consume` riêng, script hash riêng, địa chỉ riêng, và một lần công bố script tham chiếu CIP-33 riêng** — nhân với Nợ #20 (đã phải tách tx vì vượt trần byte) thì chi phí mở một vault tăng theo số vault. Bản HEAD lấy asset name theo `seed` ⟹ **N** vault dưới **một** policy = script hash của chính script, nên script hash không phụ thuộc vault nào.<br>Bên này nghiêng **HEAD**, ba lý do đo được: (i) mô hình là mỗi DID một vault ⟹ N phải là N, không phải 1; (ii) apply-param mang hash của một validator khác đúng là bẫy Q1 TAAD-hash đã ghi ở nhà LAMP — sửa validator NFT là buộc redeploy mọi thứ đứng sau; (iii) `BOUNDARIES.md` đang khai đúng công thức bản HEAD.<br>**Không tự quyết** vì hai điều chưa kiểm: bản main **đã merge** rồi, và chưa ai đối chiếu các vault đang sống trên hai testnet chạy bản nào. Hệ quả kèm theo nếu chốt HEAD: `vault_id_nft.ak` + `engage_nft.ak` xoá theo đúng ba điều kiện `BOUNDARIES.md §5`, và `#"564c54"` phải vào mục **Không được xoá** nếu có UTxO nào đang mang nó.<br>**Bổ sung 2026-08-17 — chọn HEAD KHÔNG mở lại lỗ mà bản main vá.** Lý do bản main tồn tại là một PoC thật: `poc_fabricated_magic_burns_ok` từng PASS, vì `consume.ak` nhận một UTxO là "vault" chỉ vì nó nằm ở địa chỉ vault script, nên ai cũng trả ~2 ADA để đỗ một UTxO datum bịa `current_amount: 10^18` rồi tiêu MAGIC chưa từng được sinh (`origin/main:…/vault_id_nft.ak:1-12`). Bản HEAD chặn cùng đường đó, nhưng chặn ở **validator vault** chứ không ở `consume`: `ScheduleGen/onchain/validators/vault.ak:100` — *"The single script hash IS the vault_id policy id"*; `:816-818` bắt buộc vault input mang đúng một NFT dưới policy = chính script hash của nó; `:810` ghi rõ UTxO không mang NFT thì *"cannot be spent by ANY handler"*; `:259` nêu đích danh ca datum giả. `consume.ak` HEAD vẫn chỉ kiểm `is_at_script` (`:402`) — đủ, vì UTxO giả không spend nổi ở tầng vault. Nên đây là đổi **chỗ đặt** kiểm tra, không phải bỏ kiểm tra.<br>**Khảo cổ 2026-08-17 (agent A1) — năm dữ kiện, hai trong đó sửa chính dòng này.**<br>(1) **Thứ tự sinh: bản main TRƯỚC 3 ngày.** `392ddcb9` 2026-08-06 08:37+07 (`feat/vault-id-nft`, tuanzoro2k) vs `17f051d8` 2026-08-09 15:29+07 (feat/converge). Cả hai tách từ `f36dd9b7` (30/7). Nhưng bản main **đã hạ cánh** (`296db877`) và có hai commit vá theo (`364cc449` đẩy `vault_nft_policy/name` thành apply-param #4/#5 của ScheduleGen vault, 4→6; `ca5870df` bắt SDK bê NFT sang output, `requireField` không default) — HEAD chưa có vá tương đương ở tầng off-chain.<br>(2) 🔴 **Hai bên tiến hoá ĐỘC LẬP, không đọc nhau — bằng chứng cứng, không phải suy đoán.** `refs/remotes/origin/feat/vault-id-nft` **chưa từng tồn tại trong clone này** (`git reflog show` → `fatal: unknown revision`; `grep -c vault-id-nft .git/packed-refs` → 0). `git reflog show refs/remotes/origin/main --date=iso`: `61a46f2c @{2026-08-09 22:58}` → `ed52f850 @{2026-08-12 08:29}` ⟹ object `392ddcb9` chỉ vào máy này **2026-08-12**, ba ngày SAU `17f051d8`. `grep -rn "vault_id_nft\|#27" _Agents/topics/` → không dòng nào trước 13/8. PR #27: `reviews` rỗng, `comments` rỗng — **tự merge, 0 duyệt**. Nghĩa là "đã merge" ở bản main không mang theo một chữ ký duyệt nào; và bên HEAD **cố ý bác đúng khuôn one-shot** đó (`_Agents/topics/consumemagic-orchestration.md:416-420`, 7/8: *"Khuôn one-shot `price_nft`/`engage_nft` SAI cho N-vault… cần mint state-thread N-instance, `asset_name=hash(seed)`"*) nhưng bác một THIẾT KẾ, không bác một PULL REQUEST. Điểm gãy quy trình: work-order 7/8 có mục *"kiểm Tuân/#26 (build nếu chưa)"* (`:399`) — **không tìm thấy ghi chép nào cho thấy việc kiểm đó đã chạy**.<br>(3) **PoC `poc_fabricated_magic_burns_ok` chưa từng là mã đã commit.** `git log --all -S'poc_fabricated_magic_burns_ok'` → 4 commit, **cả 4 chỉ chạm văn bản**. Nó chạy trong scratchpad phù du 4/8, output còn ở `_Agents/topics/ra-soat-toan-dien-2026-08-04.md:290` (`pass mem=556177`) — **không chạy lại được**. HEAD **có** phủ ca đó, đã chạy thật `aiken v1.1.21`: `poc_fabricated_magic_schedule_fails`, `poc_fabricated_magic_burn_batch_fails` (ScheduleGen) và `poc_fabricated_magic_instant_gen_fails`, `poc_fabricated_magic_burn_batch_fails` (InstantGen) — **4/4 pass**.<br>(4) 🔴 **SỬA chính dòng trên: mô tả "mint gộp… cùng khuôn `validate_mint_vault_id` (`consume.ak:207-224`)" là SAI.** `consume.ak:207-224` là `validate_mint_engage_id` — mint **thread NFT của Engage** (từ `04afe875`), chỉ *nhắc tên* `validate_mint_vault_id` trong chú thích. Trên HEAD, **`ConsumeMAGIC` không có bất kỳ kiểm tra vault-id NFT nào**. Nên đây là **HAI** phân kỳ chồng nhau, dòng trên mới ghi một: (a) kiến trúc vault-id NFT, (b) `engage_nft.ak` biến mất khỏi HEAD vì gộp vào `consume.ak`. Bỏ sót (b) thì lúc hoà, `engage_nft_policy/name` — 2 trong 10 apply-param của `consume.ak` bản main — **không có chỗ đi**. (Sửa nhỏ kèm theo: `#"564c54"` khai ở `vault_id_nft.ak:32`, không phải `:31`.)<br>(5) **Lợi thế thật của bản main mà dòng trên chưa cân:** main phủ chân `consume` bằng 3 test **kèm mutation test** (`origin/main:consume.ak:610,624,638` — `poc_vault_no_nft_rejected`, `_two_nft_`, `_wrong_nft_policy_`; thân PR #27 dán output: gỡ `expect … == 1` ⟹ 3 test chuyển fail). HEAD có 48 test ở `consume.ak` mà **không test nào** kiểm vault-id NFT. ⟹ nếu chốt HEAD, phải thêm việc: **port `poc_vault_*` sang dạng kiểm được ở tầng vault, hoặc chấp nhận chân `consume` không có kiểm hồi quy**.<br>(6) **Mâu thuẫn còn sống trên `origin/main`, chưa ghi ở đâu:** `scripts/config.ts:44` đọc **một** biến `VAULT_ID_NFT_POLICY_ID`, trong khi `scripts/deploy/07_create_schedule_vault.ts:152-158` **đúc một policy one-shot MỚI từ `genesisUtxos[0]` mỗi lần chạy** ⟹ vault thứ hai có policy id khác ⟹ `consume.ak`/`vault.ak` đã apply cặp cũ không nhìn thấy nó. Đây là hệ quả trực tiếp của "MVP MỘT policy / MỘT vault", và `git log --follow origin/main -- …/vault_id_nft.ak` → **đúng 1 commit, chưa sửa lần nào**; `gh api` bình luận issue #26-30 grep "một policy\|N vault\|one-shot" → **0 dòng**. Không ai xử lý hạn chế đó sau khi nó được khai.<br>**Đối chiếu CHUỖI THẬT 2026-08-17 (agent A2, Blockfrost chỉ-đọc) — điều D12 nói "chưa ai kiểm" nay đã kiểm.** Hai bản phân biệt được chỉ bằng độ dài asset name: 3 byte `#"564c54"` (main) vs 32 byte blake2b (HEAD). Đo bằng Python thẳng trên JSON Blockfrost, không gõ tay: **13/13 vault đang sống trên cả hai testnet đều 32 byte ⟹ TẤT CẢ là bản HEAD. Không một vault nào chạy bản `origin/main`.** Phân bố: Preview 5 vault ScheduleGen (`addr_test1wp7y9t8f35…n5czz`) + 4 vault InstantGen (`addr_test1wps6wdh9sh…xlxnl6`) = 9009 tLAMP; Preprod 2 + 2 = 4004 tLAMP; **tổng 13.013 tLAMP**. Policy mỗi NFT = đúng script hash của chính vault chứa nó (đối chiếu `scripts/DEPLOYED.md`: ScheduleGen Preview `7c42ace9…a49865`, InstantGen Preview `61a736e5…4abb1cf`, ScheduleGen Preprod `12368135…c60c6`, InstantGen Preprod `94c0c8b2…c76566`) — khớp mô hình HEAD. **Không vault nào thiếu NFT**: quét `/assets/{policy}744c414d50/addresses` (mọi địa chỉ đang giữ tLAMP) → mỗi mạng đúng 3 địa chỉ (2 vault script + ví deploy), 13/13 UTxO đều mang đúng 1 NFT. **`#"564c54"` không tìm thấy ở đâu**: 0 kết quả trên 13 UTxO vault + toàn bộ UTxO ví deploy (35 Preview + 10 Preprod), và `scripts/DEPLOYED.md` không có mục nào ghi deploy `ConsumeMAGIC`. `[CHƯA XÁC NHẬN tuyệt đối]` — policy của `vault_id_nft.ak` khoá theo `genesis_ref` nên không brute-force được, và A2 không quét hết 204 tx lịch sử; nhưng policy đó **không có nhánh burn** (`quantity == 1` chặn cả âm), nên nếu từng mint thì token phải còn sống ở một UTxO nào đó — không thấy. ⟹ **chưa đưa `#"564c54"` vào mục Không được xoá**, chưa có UTxO nào chứng minh nó tồn tại.<br>**Cổng migrate — trả lời dứt khoát:** chốt **HEAD** ⟹ không phải di trú gì, 13 vault đã đúng bản. Chốt **main** ⟹ 13.013 tLAMP **không kẹt vĩnh viễn**, vì `WithdrawLamp` có sẵn (`ScheduleGen/…/vault.ak:169`, `InstantGen/…/vault.ak:191`) và script đã công bố CIP-33 on-chain (bằng chứng thô: tx `8ffe6dc7…` mang input tham chiếu `reference_script_hash: 12368135c4a88018b75a7a86a3657298febcd57bfd3cad991adc60c6`, đúng bằng policy NFT vault ScheduleGen Preprod). Cardano không quan tâm nhánh git nào thắng — script CBOR đã deploy còn UTxO tham chiếu sống thì ai cũng dựng được tx rút hợp lệ. Ba điều kiện để không kẹt: (a) **không xoá source `vault.ak` bản HEAD** dù main thắng — giữ ít nhất một tag; (b) không mất SDK off-chain biết gọi `WithdrawLamp` bản HEAD; (c) UTxO tham chiếu CIP-33 không được ai tiêu, phải kiểm định kỳ. 🔴 **Nguyên tắc rút ra cho mainnet: chốt D12 TRƯỚC khi mở vault mainnet đầu tiên, và không bao giờ xoá source của validator đã từng nhận LAMP thật — chỉ ngừng dùng nó để tạo vault MỚI**<br>**ĐO CHI PHÍ eUTXO 2026-08-17 (agent A3, `utxo-expert`)** — đo trên `origin/main`=`ed52f850` và `HEAD`=`b8b5698a` trong worktree riêng, `aiken v1.1.21+42babe5`, `node v24.11.0`; `git diff --stat b8b5698a 141f8056 -- ConsumeMAGIC ScheduleGen scripts` **rỗng** nên số vẫn đúng với nhánh hôm nay. Hằng mạng dùng làm đầu vào (không phải số đo): `maxTxSize=16384`, `coinsPerUTxOByte=4310`, `maxTxExUnits={14M mem, 10G steps}`, `minFeeRefScriptCostPerByte=15`.<br>**(a) Trục quyết định là N script hash so với 1** — không phải byte, không phải phí. main tự khai *"1 deploy / 1 thread"* (`origin/main:…/engage_nft.ak:15-18`) và `07_create_schedule_vault.ts:152-186` xác nhận mỗi lần tạo vault sinh `genesis_ref` mới → policy mới → `applyParamsToScript` → **script hash mới, địa chỉ mới**. HEAD: `07_create_schedule_vault.ts:180` `toUnit(vaultScriptHash, vaultIdName)` — một script, N vault, một địa chỉ. Bảng: script hash vault **N vs 1**, script hash consume **N vs 1**, tx công bố ref-script **2N vs 2**. ADA khoá (ref-script + vault 1-batch + Engage): N=1 → 65,46 vs 75,71 (main rẻ hơn 0,86×); N=16 → 1.047,36 vs 133,24 (**7,86×**); N=32 → **2.094,73 vs 194,62 (10,76×, chênh 1.900 ADA)**. **Điểm hoà N ≈ 1,2** — từ vault THỨ HAI trở đi HEAD rẻ hơn. Và tệ hơn tiền: mô hình là mỗi DID một vault, nên "1 deploy / 1 thread" là **trần vận hành**, mỗi người dùng mới cần một lần deploy validator.<br>(b) 🔴 **Cả hai bản HIỆN ĐANG HỎNG so với trần 16.384 byte, hỏng hai kiểu khác nhau.** Kiểm chứng chéo phương pháp: `scripts/deploy/06_publish_ref_scripts.ts:5-7` của chính kho ghi 17.303 B **đo thật trên Preview**; phép lắp CBOR của A3 cho 17.310 B — lệch 7 byte. · **HEAD với witness inline: 17.310 B ngay ở vault RỖNG** ⟹ **không tx consume nào dựng nổi ở bất kỳ kích thước datum nào**, mà `ConsumeMAGIC/offchain/src/consume.ts:256-257` vẫn đang `attach.SpendingValidator`. Với HEAD, reference script **không phải tối ưu hoá — là điều kiện sống**; hạ tầng đã có (`06_publish_ref_scripts.ts`), chỉ thiếu sửa off-chain sang `readFrom`. · **main dựng được tới batch 19 (16.379 B, dư 5 B), chết từ batch 20 (16.455 B)** trong khi cưỡng chế on-chain cho phép `MAX_BATCHES_PER_VAULT=32` ⟹ đúng ca "cap đặt cao hơn trần thật": **vault main vượt 19 batch là LAMP kẹt vĩnh viễn**, và main **không có** bộ công bố ref-script nào để thoát (`scripts/deploy/` bản main chỉ có `01,03,07,08`). Vá được, nhưng phải nhân N lần theo (a).<br>(c) **Đường thoát khi kẹt:** HEAD đỗ ref-script ở địa chỉ native `sig` của chủ khoá (`06_publish_ref_scripts.ts:57-58`) ⟹ ADA thu hồi được; main chưa viết bãi đỗ nào, và chi phí thoát cũng nhân N.<br>(d) **Gộp mint vào validator KHÔNG làm nhỏ đi:** `consume` 3.978 → 4.962 (+984 B), `vault` 9.975 → 11.299 (+1.324 B), đổi lại bỏ hai script 459 B **vốn không bao giờ nằm trong tx consume**. Ròng **+2.308 B (+16,5%)** mỗi tx consume.<br>(e) **NHIỄU — không được lái quyết định:** min-ADA chênh **+0,2974 ADA/cặp (vault+thread)** do tên NFT 32 B thay vì 3 B (delta vault là hằng **+129.300 lovelace** ở mọi cỡ datum, 0/0 đến 32/64); ở N=32 tổng 9,5 ADA = **0,45%** khoản 1.900 ADA main khoá thêm. ExUnit cộng consume+vault xấu nhất: main 2,72 M mem / 905,4 M steps (19,4% / 9,1% trần) vs HEAD 3,82 M / 1.245,98 M (27,3% / 12,5%) — còn dư 10 M mem, không bên nào tới gần. Phí ref-script Conway chênh **+0,0346 ADA/tx**. Tranh chấp UTxO **không** phân kỳ: price beacon là reference input `readFrom` ở cả hai; N vault chung một địa chỉ ở HEAD không tạo tranh chấp (UTxO khác nhau, tiêu song song). `shard`/`shard_nft` hash trùng khớp cả hai cây.<br>(f) **Test:** `aiken check` non-Legacy main **125 pass** vs HEAD **322 pass**; vitest `ConsumeMAGIC/offchain` 42 vs 67, `ScheduleGen/offchain` 33 vs 41. Dữ kiện kèm: HEAD **hỏng nếu `npm ci` một phát** ở `ConsumeMAGIC/offchain` (`sh: tsc: command not found`) — phải cài bottom-up `ProtocolUtils` → `pricing` → `offchain`; cổng CI mới dựng phải theo thứ tự đó.<br>**A3 KHÔNG đo được:** phase-2 validation (byte chính xác, "ledger nhận" thì chưa chứng minh); ExUnit thật trong ScriptContext đầy đủ (số `aiken check` là **cận dưới**); worst-case ExUnit của main (main không có ca tương đương `consume_worst_case_full_table_two_engage_happy`, nên so worst-case là so hai bộ test khác nhau). `[CHƯA XÁC NHẬN]` `consume.ak:119` `script_inputs_confined_to` — nếu đúng thì HEAD **không gộp được** consume với Paymaster/GetMAGIC trong một tx.<br>**Hệ quả thứ hai nếu chốt HEAD** (bổ sung cho hệ quả port `poc_vault_*` ở (5)): off-chain **bắt buộc** chuyển từ `attach.SpendingValidator` sang `readFrom` reference script **trước khi** dựng được tx consume đầu tiên.<br>**ĐỢT 2 — A4 (`adversary`) tấn công kết luận sơ bộ, 2026-08-17. Bốn trong năm mệnh đề phải sửa.** Giới hạn A4 tự khai: chỉ có Read/Grep/Glob, **không đọc được `origin/main`** — mọi câu về main là `[CHƯA ĐO]` dựa trên trích dẫn D12; mọi câu về HEAD đọc trực tiếp. Bên này **kiểm lại tay** hai phát hiện nặng nhất, đúng hết ⟹ đã tách thành **Nợ #30** và **Nợ #31**. **Ba lệnh `[CHƯA ĐO]` A4 để lại đã chạy xong** (chi tiết trong Nợ #30): main có `coalesce_holdings` ở **cả** on-chain (`lock.ak:84-102`, gọi tại `:67`) lẫn off-chain (`ProtocolUtils/src/index.ts:222`), kèm 3 test hồi quy và một chú thích mô tả đúng lỗi (`vault.ak:1296-1299`) ⟹ luận điểm (a) của A4 **được xác nhận bằng số đo**, không còn là suy luận.<br>· 🔴 **Mệnh đề 5 ĐẢO DẤU.** "main giữ ba lợi thế phụ, port là xong" sai theo ba kiểu: (a) `coalesce_holdings` **không phải tiện ích mà là bản vá cho lỗi đóng-băng-tiền HEAD đang mang** → Nợ #30; (b) 3 test `poc_vault_*` **KHÔNG port được** vào ConsumeMAGIC — `ConsumeMAGIC/onchain/aiken.toml` chỉ phụ thuộc `aiken-lang/stdlib`, không phụ thuộc ScheduleGen, mà trên HEAD việc từ chối vault-không-NFT nằm ở `ScheduleGen/…/vault.ak:815-822` ⟹ **món này CÓ đi theo lựa chọn kiến trúc**, trái đúng câu của mệnh đề; cái main thật sự có mà HEAD không có là **kỷ luật mutation test**, một thứ quy trình; (c) `unlockLockedAmount` HEAD **đã có** (`ScheduleGen/offchain/src/math.ts:36`), chỉ thiếu bản trong `ProtocolUtils` ⟹ chuyện trùng lặp/vị trí, rủi ro thật là hai bản trôi khỏi nhau (P8). ⟹ "ba lợi thế" thực chất là **một lỗi phải sửa + một món không port được + một món trùng lặp**.<br>· **Mệnh đề 3 gãy ở khung, không ở số.** (i) **"Mỗi DID một vault" KHÔNG được cưỡng chế ở đâu cả** — `validate_mint_vault_id` (`vault.ak:837-923`) là permissionless, cùng một `owner` pkh đúc được N vault ⟹ tiền đề của mệnh đề 3 là **việc phải làm thêm**, không phải dữ kiện. (ii) **Trục N-vs-1 cắt NGƯỢC**: nó vừa là trục chi phí (HEAD thắng) vừa là trục **bán kính vụ nổ** (main thắng) — HEAD một script hash ⟹ một lỗi validator giết **mọi** vault, một UTxO ref-script đỗ ở native `sig` của **một** khoá (`06_publish_ref_scripts.ts:56-57`) ⟹ khoá đó tiêu UTxO là **toàn hệ dừng**; main bán kính = 1 người dùng. Bảng của A3 **không có cột này**. (iii) **Kiến trúc thứ ba cả hai bên bỏ sót**: giữ một-script của HEAD nhưng `asset_name = blake2b_256(owner_pkh)`, tính duy nhất do shard ghi sổ (shard đã một-shot, đã bắt buộc co-spend ở commit — `vault.ak:233`, `:294`) ⟹ được cả ba: N vault/1 script hash, "1 DID = 1 vault" thành **bất biến on-chain**, và giết luôn Nợ #31. Giá: mint phải co-spend shard.<br>· **Mệnh đề 2 yếu tới mức phải viết lại.** (a) **Bằng chứng vòng tròn** — 13 vault do chính tác giả HEAD tạo bằng chính script HEAD; chạy script bản main thì có 13 vault kiểu main. Đó là dấu vết **ai chạy script gần nhất**, không phải phiếu bầu kiến trúc. (b) **Diện quét không phủ được kết luận** — A2 quét địa chỉ đang giữ tLAMP + ví deploy; ba loại vật chứng kiểu-main nằm ngoài diện đó: Engage thread NFT (chỉ ADA + NFT, **không bao giờ** giữ tLAMP), vault kiểu-main đã rút hết, token trong ví tuanzoro (khác khoá deploy). "Còn sống ở đâu đó chưa quét" ≠ "chưa từng tồn tại". (c) 🔴 **Thực địa im lặng đúng chỗ cần nó lên tiếng** — 13 vault chứng minh đường **genesis vault** chạy, nhưng theo A3 chưa **một** tx consume nào của HEAD từng dựng được ở bất kỳ đâu, mà consume chính là chỗ hai kiến trúc lệch nhau nhất (7 vs 10 apply-param, `engage_nft.ak` mất). ⟹ viết lại thành: *"Không vault kiểu-main nào đang giữ tLAMP; 13 vault giữ tLAMP đều kiểu HEAD. Đây là dấu vết vận hành, không phải phiếu bầu kiến trúc, và nó không nói gì về đường consume — đường mà CẢ HAI bản chưa từng chạy trên chuỗi."*<br>· **Mệnh đề 4 yếu, không gãy.** "HEAD sửa off-chain một lần" là **ước lượng, chưa phải số đo** (phase-2 chưa ai chạy), và bản vá đó **đẻ ra một quyền mới** chưa có trong bảng quyền: UTxO ref-script duy nhất đỗ ở native `sig` một khoá. A4 **xác nhận** `script_inputs_confined_to` chỉ duyệt `tx.inputs`, **không** chạm `reference_inputs` (`ConsumeMAGIC/onchain/lib/magiclamp/consume/util.ak:104-118`) ⟹ `readFrom` không bị nó chặn — gỡ được `[CHƯA XÁC NHẬN]` của A3 một nửa.<br>· **Mệnh đề 1 đứng, nhưng bất đối xứng.** Cùng thước "0 duyệt": HEAD có 15 commit local chưa đẩy, 0 review, PR #30 `CONFLICTING`. "0 duyệt" là điểm CHUNG ⟹ lý do nghi ngờ **cả hai**, không phải lý do nghiêng về HEAD. Điểm bất đối xứng thật còn lại: **main là nhánh mặc định** — thứ mọi clone mới, mọi CI, mọi người mới nhận được.<br>· **Hai chỗ HEAD ĐỨNG VỮNG** (A4 tấn công không thủng, ghi lại để không phải hỏi lần nữa): (1) *"Có đường nào tiêu Engage/consume mà không đi qua validator vault?"* → **KHÔNG** — `required > 0` (`consume.ak:158`) ⟹ `total_burned == total_required > 0` (`:171`) ⟹ `vault_refs` khác rỗng ⟹ mỗi vref **phải** là input tại `vault_script_hash` với redeemer `BurnBatch` (`:400-403`) ⟹ validator vault chạy ⟹ `validate_vault_value` chạy (`vault.ak:503`) ⟹ NFT bắt buộc. Bào chữa "kiểm đã dời xuống tầng vault" **đúng**. (2) *"Vault A mang NFT của vault B?"* → **KHÔNG** — `validate_vault_value` lấy `nft_name` từ chính input và đòi output cùng (policy, name) (`:816-818`), một địa chỉ chỉ 1 input (`:151`) và 1 output (`:779`).<br>· **Đường tấn công vào main mà A4 nêu** (`[CHƯA ĐO]`, cần Bash để dựng lại): trả lời lead "trộn NFT vault A với engage NFT thread B" → **không mất tiền, nhưng kẹt** — ghép cặp policy là **quy ước off-chain lúc apply**, không gì trong mã buộc hai policy thuộc cùng một người; trộm không thành vì `validate_burn_batch` đòi chữ ký `owner`/`personal_delegate`. Cộng: trôi cặp policy giữa deploy #1 và #2 (đã ghi ở mục (6)), và "1 deploy / 1 thread" biến người giữ khoá deploy thành **người gác cổng** — một dòng đặc-quyền chưa có trong bảng quyền.<br>· **Nhỏ, không mất tiền:** `blake2b_256(cbor(seed))` không đơn nhất toàn cục — cùng một seed, trong **một** tx, đúc được NFT **cùng tên** dưới ≥3 policy khác nhau (vault ScheduleGen, vault InstantGen, thread Engage), vì mỗi validator chỉ soi `assets.tokens(tx.mint, policy_id)` của riêng nó (`vault.ak:849-852`, `consume.ak:222-225`). Chỉ mục off-chain nào khoá theo **tên** thay vì theo **unit** sẽ đụng nhau.<br>· **Giá kiến trúc chưa ai ghi:** `script_inputs_confined_to` ghim consume vào **một** `vault_script_hash` ⟹ cần **một instance consume cho mỗi LOẠI vault** ⟹ thread Engage của một người bị tách theo loại vault, nên bộ đếm `consumed_nanogic` — trục mà `INV-MAGIC-CITIZEN` dựa vào để thưởng — **phân mảnh theo instance**. Hệ quả thiết kế, không phải lỗi.<br>🔴 **KẾT LUẬN SƠ BỘ SAU ĐỢT 2 (vẫn CHƯA phải quyết định):** vẫn nghiêng **HEAD**, nhưng **đổi lý do**. Không phải vì "thực địa đã chọn" (vòng tròn) và không phải vì "phân kỳ ở đúng một trục" (trục đó cắt hai chiều). Chỉ còn **hai** lý do đo được: điểm hoà chi phí **N ≈ 1,2** của A3, và bẫy apply-param-mang-hash-validator-khác mà main **tự khai** (`vault_id_nft.ak:14-16`) và chưa ai xử (mục (6)). Và ba việc dưới đây là **điều kiện tiên quyết**, không phải hạng mục "port sau": (1) gộp holdings ở tầng validator (Nợ #30); (2) đổi mọi phép đếm input/output của vault sang **payment credential** (Nợ #31); (3) đưa "1 DID = 1 vault" thành bất biến on-chain thay vì giả định. **Và chừng nào chưa dựng nổi MỘT tx consume HEAD qua phase-2 trên testnet thì D12 chưa đủ dữ kiện để chốt** — vì đúng phần đó mới là chỗ hai kiến trúc thật sự khác nhau.<br>**ĐỢT 2 — A5 (`auditor` lĩnh vực cardano) lập BẢNG ĐẶC QUYỀN hai bản, 2026-08-17. Trục này chưa bản nào của D12 có.** Câu hỏi A5 đặt khác các agent trước: không hỏi "bản nào rẻ hơn / có trước", mà hỏi **"mỗi bản trao cho ai quyền làm gì mà người dùng không cản được"**. A5 chạy 10 test PoC trong bản sao scratchpad (cây làm việc `git status --short` **rỗng**, đã kiểm). Bên này **kiểm lại tay từng neo bằng `git show origin/main:…`** — mọi neo đối chiếu được đều đúng; những gì chưa kiểm ghi rõ ở cuối.<br>· **Sáu đặc quyền `origin/main` để mở mà HEAD đóng** (đã tự kiểm 5/6): **Δ2 — người giữ NFT có quyền phủ quyết VĨNH VIỄN nhánh `WithdrawLamp`**: `origin/main:ScheduleGen/…/vault.ak:107` `expect vault_nft_preserved(tx.outputs, …)` và thân hàm `:677-685` chỉ đọc **output** (`find_vault_output_raw` rồi `quantity_of == 1`) — **input không bao giờ bị kiểm** ⟹ NFT ở đâu không quan trọng, nhưng output buộc phải mang nó; ai giữ NFT là giữ chìa. HEAD ghim NFT ở chính input (`:816-818`). **Δ3 — không cưỡng chế genesis sạch**: `origin/main:ConsumeMAGIC/…/vault_id_nft.ak` mint chỉ có ba `expect` (tiêu `genesis_ref`, `dict.size(own_tokens) == 1`, `quantity_of(tx.mint, policy_id, name) == 1`) và **không chạm `tx.outputs`** ⟹ người đúc đặt datum khởi tạo tuỳ ý, gửi NFT đi đâu tuỳ ý. **Δ4 — dải giá tự khai**: `origin/main:…/pricing.ak` `valid_param` kẹp `demand_mult` giữa `pp.m_min`/`pp.m_max` — **hai trường của chính datum committee ghi** ⟹ kẹp vào chính mình, không chặn gì; HEAD ghim `pp.m_min == m_min_q && pp.m_max == m_max_q` (`pricing.ak:108-140`). **Δ5 + Δ6 — beacon giá**: main chỉ có `count_sigs(committee, tx.extra_signatories) >= threshold` và `out_datum.epoch > datum.epoch` ⟹ (Δ5) epoch không có trần trên, post một `epoch = 2⁶³` là **beacon chết vĩnh viễn, mọi tx consume toàn hệ dừng**; (Δ6) `threshold` không có sàn, đặt `threshold = 0` hoặc committee trùng tên là **multisig giả**. HEAD có đủ `expect threshold > 0`, `threshold <= list.length(committee)`, `util.all_distinct(committee)`, `out_datum.epoch <= util.get_epoch(tx, ms_per_epoch)` (`price_param.ak:50-60, 92-98`). **Δ1** (hash NFT không tái lập được từ dữ liệu công khai ở bản main, vì tên cố định `#"564c54"` không ràng vào seed) — **bên này CHƯA tự kiểm**, ghi theo A5.<br>· **Một đặc quyền HEAD ĐẺ RA mà main không có — Δ8, nhẹ hơn và tự gỡ được**: UTxO ref-script CIP-33 đỗ ở địa chỉ native `sig` của khoá deploy (`06_publish_ref_scripts.ts:57-58`) ⟹ khoá đó tiêu UTxO là mọi tx consume `readFrom` gãy. Khác sáu cái trên ở chỗ **tiền không kẹt**: công bố lại ref-script là chạy tiếp. Trùng với trục "bán kính vụ nổ" A4 nêu ở mệnh đề 3(ii) — hai agent độc lập chỉ vào **cùng một** điểm, nên đây là điểm yếu thật của HEAD chứ không phải nhiễu. Dữ kiện kèm, đã tự kiểm: `git ls-tree -r --name-only origin/main -- scripts/deploy` → **chỉ `01_mint_lamp.ts` `03_deploy_shards.ts` `07_create_schedule_vault.ts` `08_deploy_getmagic.ts`** ⟹ main **không có** `06_publish_ref_scripts.ts` (nên không mang Δ8) và **không có** `09_deploy_consume.ts` (nên chưa từng có đường deploy ConsumeMAGIC — khớp với A2: `DEPLOYED.md` không có mục ConsumeMAGIC nào).<br>· 🔴 **Ba lỗ hổng KHÔNG phân kỳ — có ở CẢ HAI bản, nên chốt D12 kiểu gì cũng phải vá** (đã tự kiểm cả ba, tách thành Nợ #32/#33/#34): trần trên của `base_price` không tồn tại; datum khởi tạo shard không ràng người deploy (`shard_nft.ak` **giống hệt nhau từng byte**, `git diff origin/main HEAD -- ScheduleGen/onchain/validators/shard_nft.ak` **rỗng**); `oracle_vkey` của GetMAGIC là trường của chính datum nó xác minh.<br>· **Một dữ kiện làm nặng thêm Nợ #31, đã tự kiểm hai bên**: nhánh fire chỉ ghi `shard_out.shard_locked_lamp == shard_in.shard_locked_lamp - lamp_released` — **không có `>= 0`** — ở `HEAD:vault.ak:442` **và** `origin/main:vault.ak:374`. Nhánh commit thì có trần trên (`:307-308` HEAD / `:251-252` main) nhưng không có sàn dưới. Nghĩa là con đường "đếm xuống âm" của Nợ #31 **không có lưới thứ hai ở bản nào**, và một dòng `expect … >= 0` sẽ hạ thiệt hại #31 từ "nới headroom vĩnh viễn" xuống "tx bị từ chối".<br>· **Ý nghĩa cho D12:** bảng đặc quyền **nghiêng về HEAD và không vòng tròn** — nó đọc mã nguồn hai bên, không đọc dấu vết ai chạy script gần nhất, nên nó **không** dính lỗi mệnh đề 2 mà A4 chỉ ra. Nhưng nó **không** thay được cổng A4 đặt: sáu Δ trên đều nằm ở nhánh `WithdrawLamp`/`PostPrice`/mint-genesis — **không cái nào nằm ở đường consume**, đúng chỗ hai kiến trúc lệch nhau nhất và cả hai chưa từng chạy trên chuỗi.<br>· **Đóng nốt phần `[CHƯA ĐO]` của A5, 2026-08-17.** **20/20 test `price_param` bản HEAD — ĐÚNG**: `grep -c '^test ' ConsumeMAGIC/onchain/validators/price_param.ak` → **20**, và `aiken check -m price_param_` → *"20 tests | 20 passed | 0 failed · Summary 20 checks, 0 errors, 0 warnings"*. **Δ1 — đúng ý, nhưng câu chữ A5 quá mạnh, viết lại**: tên asset bản main là **hằng** `pub const vault_id_nft_name: ByteArray = #"564c54"` (`origin/main:…/vault_id_nft.ak:32`), không hàm nào của seed; ràng buộc one-shot nằm ở **policy id** (`genesis_ref` là apply-param). Nên không phải "không tái lập được" — có chỉ mục chuỗi thì từ policy id vẫn dò ra tx mint và `genesis_ref` trong input của nó. Cái **thật sự mất** là: tên token **không phân biệt vault nào với vault nào** — N vault main đều mang token tên `VLT`, khác nhau đúng ở policy id, mà policy id chỉ lấy được từ hồ sơ deploy. Và hồ sơ deploy bản main giữ **đúng MỘT** ô: `origin/main:scripts/config.ts:44` `vault_id_nft: process.env.VAULT_ID_NFT_POLICY_ID` — một biến, cho mọi vault ⟹ Δ1 **không phải phát hiện độc lập**, nó là **cùng một lỗ với mục (6)** nhìn từ phía chỉ mục. HEAD ngược lại: `nft_name = crypto.blake2b_256(cbor.serialise(seed))` (`ScheduleGen/…/vault.ak:888`) — tên **là** cam kết vào seed, kiểm bằng một phép băm. (Đừng đọc thành "HEAD có tên toàn cục duy nhất" — mục "nhỏ, không mất tiền" ở trên đã ghi cùng seed đúc được cùng tên dưới ≥3 policy.) 🔴 **F-9 và F-10 không kiểm được và sẽ không kiểm được**: báo cáo A5 **không được lưu ra tệp** (scratchpad chỉ có `a3-report.md`), nên hai mã đó giờ là **tên trống — không ai biết chúng khẳng định điều gì**. Không được trích chúng như bằng chứng; muốn có lại phải chạy lại A5. **Bài học quy trình**: báo cáo agent phải ghi ra tệp trước khi ghi tóm tắt vào đây, đúng như A3 đã làm. (Số đếm "10 test PoC / 5 lần `aiken check`" là quy trình nội bộ của A5, không phải dữ kiện quyết định — bỏ khỏi danh sách phải kiểm.) Ghi chú công cụ của A5, đã dùng lại và đúng: `aiken check -m <X>` lọc theo **tên TEST**, không theo tên module; và `placeholder` của Aiken có `validity_range` vô hạn ⟹ test đánh dấu `fail` xanh giả — phải đặt `interval.between(0, 1_000)` rồi đọc dòng trace `x <expected>` mới chắc test rớt đúng chỗ định rớt |
| D13 | Bản `SPEC/MagicLamp-Tripletoken-Feat-(Vi).md` nào là gốc — PR #23 hay HEAD? (Nợ #28) | Hai nhánh sửa cùng một gốc `585e05d7` theo hai hướng, và mọi tài liệu khác đều trỏ về **tên tệp** này chứ không trỏ về nội dung nào. Không tự hoà vì hoà spec bằng tay là tự viết lại spec — việc đó phải là quyết định của chủ nhân, không phải kết quả của một lần `git merge`. Cần chốt: lấy một bên làm gốc rồi ghép tay phần còn lại của bên kia, và ghi ngày + commit gốc vào đầu tệp để lần sau đo được |
| D2 | `ScheduleGen` cho suất ≈0,0113 MAGIC/LAMP/epoch, spec chốt ρ=1 — chính sách siết thêm hay drift? | spec cho phép lớp dịch vụ siết dưới trần, nhưng trần `⌊L_avail×RATE_REF_Q/Q⌋` thì không tồn tại ở đâu trong ScheduleGen |
| D3 | `did_commit` lúc genesis: để tự do hay ép rỗng? | ép rỗng khoá chết đường bind DID PhoenixKey sau; mở thì người đúc đặt được `did_commit` của người khác |
| D4 | `AppEconomics` · `ProfileChange` · `Consolidate` — hội tụ hay xoá? | chờ chốt PM/tư-cách trong spec canonical; dọn trước là dọn mù |
| D5 | `_appeconomics_legacy.ts` + 31 ca test bám vào nó | buộc chung số phận với `AppEconomics` (D4) — nó là bản sao math của module đó |
| D6 | `MagicSDK/V1_TESTNET_PLAN.md` — viết lại theo 2 vault hay xoá? | chưa deploy mạng nào; viết ma trận nghiệm thu bây giờ là viết mù lần hai |
| D7 | `MAX_PRICE_STALE = 1` có đúng ý định? | mainnet 1 epoch = 5 ngày ⇒ `stale=1` là chấp nhận giá trễ 5 ngày. Và **chưa có keeper** post lại giá ⇒ hệ tự khoá sau 1 epoch |
| D8 | Siết `personal_delegate` + bắt buộc co-spend ở `BurnBatch`? (Nợ #14) | Đụng validator đang sống VÀ đụng lược đồ datum. Hai việc, hai mức: (a) đòi ≥1 input ở `engage_script_hash` khi người ký là delegate — chỉ đổi logic, không đổi datum; (b) đổi `personal_delegate` từ `Option<ByteArray>` sang bản có `max_nanogic_per_epoch`/`expires_epoch`/`allowed_op_types` — đổi datum, trường mới phải THÊM Ở CUỐI để không lệch chỉ số constructor. Không tự quyết vì (b) là thay đổi mô hình uỷ nhiệm, không phải sửa lỗi |
| D9 | `PRICE_THRESHOLD` mặc định 1 — giữ hay bắt buộc M-of-N trước mainnet? | Beacon `PriceParam` là **reference input** của mọi tx Consume. Một khoá committee chèn `PostPrice` hợp lệ ngay trước là beacon bị chi, tx nạn nhân invalid từ gốc, lặp không giới hạn ⇒ **chặn dịch vụ nhắm đúng một người**. PhoenixKey đặt thành điều kiện W2 cho `op_type=7 did.rotate` (xoay khoá sau khi nghi lộ) — ở đó nó là chặn quyền tự vệ. Ghi chi tiết ở `ConsumeMAGIC/EXEC.md §4` |
| D10 | `op_type` nào được **giá cố định** (không nhân `demand_mult`)? | `demand_mult` tính từ `ops_served_epoch` gộp toàn hệ nên tải của module không liên quan định giá thao tác an ninh của DID, và một đợt lộ khoá hàng loạt làm Rotate đắt lên đúng lúc cần rẻ. Hai hình dạng: cờ `fixed` trong `OpPrice` (đổi lược đồ datum beacon đang sống, phải post lại mọi beacon) hoặc **dải `op_type` quy ước** (chỉ đổi hàm giá, không migrate). MAGIC nghiêng về dải quy ước. Chi tiết + đính chính "giá cố định thì khỏi đọc beacon" (SAI): `ConsumeMAGIC/CONTRACT.md §A` |

---

## Bất biến đang được cưỡng chế (đừng phá khi sửa)

- **INV-VAULT-IDENTITY** — vault **sống** (`InstantGen`, `ScheduleGen`) mang một NFT one-shot
  sinh cùng lúc với vault (`asset_name = blake2b_256(cbor.serialise(seed))`, policy = chính
  script hash). Không có NFT thì không nhánh spend nào đi qua. Bịt lỗ "đặt UTxO giả ở địa chỉ
  script với datum bịa". *Hai validator mồ côi chưa được nối — xem mục Mồ côi.*
- **P8** — toán Aiken và TypeScript trùng bit, vector chuẩn trong `tests/` là trọng tài.
- **Σburns == required** (ConsumeMAGIC) — dấu bằng, lệch một nanogic là tx bị từ chối.
  ⚠️ **MỘT CHIỀU.** Bất biến này sống trong `consume.ak`, nên nó chỉ tồn tại ở tx CÓ
  ConsumeMAGIC co-spend. `validate_burn_batch` của vault không đòi ConsumeMAGIC có mặt —
  xem Nợ #14. Đừng đọc dòng này thành "MAGIC chỉ giảm khi có dịch vụ".
- **`halved` KHÔNG phải bất biến.** Không có `expect` nào từ chối `halved == True`;
  validator chỉ không bao giờ SINH ra True. Chú thích cũ trong `types.ak` ghi "Any True
  value is rejected" — đã đính chính 2026-08-12.
- **Chỉ số constructor Plutus Data là hợp đồng nhị phân** — variant đã bỏ khỏi mô hình
  (`BatchSource::Snapshot`, `::Vacuum`) và trường `vacuum_orders` vẫn phải nằm nguyên chỗ cũ.
- **`lamp_asset_name` là apply-param #2 của mọi vault**, suy ra từ mạng, không hardcode.

---

## Không được xoá

> Mục cố định theo `_rules/agent-hygiene.md §4`. Chủ nhân đã **bãi bỏ `Legacy/`** ngày
> 2026-08-09: tệp lỗi thời thì **xoá**, không dồn sang đó. `Legacy/` đang có thì để yên, xoá
> dần khi đụng tới. Nguyên tắc chung: **định danh đã lên chain không bao giờ là tệp lỗi thời.**
>
> Danh sách này tồn tại vì `git log` trả lời được "cái gì đã bị xoá" nhưng không trả lời được
> "cái gì trông giống thứ đã xoá mà đừng đụng vào".

| Thứ | Nghe như chết vì | Thật ra sống ở đâu | Vỡ gì nếu bỏ |
|---|---|---|---|
| `UMKeeper/` | tên module nghe như tiện ích phụ | apply-param #3/#4 của `InstantGen/onchain/validators/vault.ak`; ref input UM bị ghim theo `um_script_hash` | InstantGen mất hệ số cầu mạng — công thức thưởng PHA-2 không tính được |
| hằng `snapshot_base_rate_q` | có chữ "snapshot", mà `SnapshotGen` đã chết | `ScheduleGen/onchain/validators/vault.ak` (`compute_rate_locked_q`, `baseline_at_commit_q`) + bản off-chain `SNAPSHOT_BASE_RATE_Q` | ScheduleGen mất mốc suất khoá lúc commit |
| variant `BatchSource::Snapshot` (0) và `::Vacuum` (2) | hai cửa sinh đó đã bỏ khỏi mô hình | `types.ak` của 4 module + `types.ts` tương ứng | 🔴 **chỉ số constructor đã lên chain** — đánh lại số là vỡ decode **mọi UTxO đã tạo** |
| trường `vacuum_orders` (index 6 của `VaultDatum`) | tên trỏ về cửa đã chết | 4 module vault, đúng vị trí thứ 7 | như trên — lệch vị trí là lệch toàn bộ trường phía sau |
| trường `halved` (index 8 của `MagicBatch`) | tự khai "DEAD FIELD", luôn `False` | `InstantGen/.../types.ak` | giữ hình dạng 9 trường tương thích byte với datum đã deploy |
| tag `preserve/prepaidgen-stash-2026-07-30` | trông như tag rác | neo 24 tệp nguồn `PrepaidGen` đang treo trong `refs/stash@{0}` | `git stash clear/drop` sẽ xoá thật cây nguồn đó — xem "Còn nợ #5" |
